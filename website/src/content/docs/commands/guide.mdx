---
title: bonsai guide
description: View bundled terminal cheatsheets — quickstart, concepts, CLI reference, and the custom files guide.
---

import { Aside } from '@astrojs/starlight/components';

## Synopsis

```
bonsai guide [topic]
```

## Description

`bonsai guide` renders a bundled cheatsheet directly in your terminal using [Glamour](https://github.com/charmbracelet/glamour) markdown rendering. The cheatsheets are embedded in the Bonsai binary alongside the catalog and are designed as quick terminal companions to the full Starlight documentation.

Run `bonsai guide` with no arguments to open an interactive picker. Pass a topic directly (`bonsai guide <topic>`) to skip the picker and render it immediately. Passing an unknown topic prints an error and exits non-zero. Passing more than one argument is rejected by Cobra with an "accepts at most 1 arg" error.

Each cheatsheet ends with a deep-link back to the full web guide at [laststep.github.io/Bonsai](https://laststep.github.io/Bonsai/) for longer-form reading.

## Topics

| Topic | What it covers |
|:------|:---------------|
| `quickstart` | Post-install 5-step walkthrough — what to do after `bonsai init` |
| `concepts` | The mental model — station, instruction stack, agents, sensors, routines, scaffolding |
| `cli` | One section per command (`init`, `add`, `remove`, `list`, `catalog`, `update`, `guide`) |
| `custom-files` | BONSAI frontmatter format, file naming, directory placement, examples |

<Aside type="tip">
The guide output adapts to your terminal's color scheme. Glamour auto-detects light or dark and renders accordingly. YAML frontmatter in the bundled markdown is stripped before rendering.
</Aside>

## Flags

This command has no flags.

## Examples

**Open the interactive picker:**

```bash
bonsai guide
# ? Pick a guide
#   > Quickstart — 5-step post-install walkthrough
#     Concepts — the mental model
#     CLI — command-by-command reference
#     Custom Files — add your own abilities
```

Use the arrow keys and press `Enter` to render the selected topic.

**Render a topic directly:**

```bash
bonsai guide quickstart
bonsai guide concepts
bonsai guide cli
bonsai guide custom-files
```

**Unknown topic:**

```bash
bonsai guide foobar
# Error: unknown topic "foobar". Available: quickstart, concepts, cli, custom-files
```

Exits non-zero.

**Pipe to a pager for long output:**

```bash
bonsai guide custom-files | less -R
```

The `-R` flag preserves ANSI color codes when piping through `less`.

## See Also

- [`bonsai update`](/Bonsai/commands/update/) — detect and track custom files after creating them
- [Your First Workspace](/Bonsai/guides/your-first-workspace/) — the full quickstart guide on the web
- [How Bonsai Works](/Bonsai/concepts/how-bonsai-works/) — the full concepts guide on the web
- [Creating Custom Skills](/Bonsai/guides/creating-custom-skills/) — detailed web guide for custom skills
