---
tags: [playbook, status]
description: Live task tracker. Update this file at the start and end of every working session.
---

# Bonsai — Status

> [!note]
> Move items between tables as work progresses. Link to the relevant plan in `Plans/Active/` or `Plans/Archive/`. Older Done items age out to `StatusArchive.md`.

---

## In Progress

| Task | Plan | Agent | Notes |
|------|------|-------|-------|
| Plan 31 — v0.3 release readiness bundle. **PR1 shipped** (PR #75 squash `48e2678`) — peer-awareness silent refresh + bonsai-model skill + .bonsai/catalog.json snapshot + CLAUDE.md Bonsai Reference block. Fix commit `e3bdd38` addressed 4 review minors (AgentWorkspace refactor to use buildAgentTemplateContext, nil-guard, DocsPath fallback drop, 3 regression tests). Review: code PASS-WITH-MINORS + security PASS-WITH-MINORS (0 blockers each; non-blocking items filed as Plan-31-cosmetic/-test-gap/-security-hardening). 8/8 CI green. **PR2 next:** remove cinematic + update cinematic + catalog --json + NO_COLOR + hints 3-layer, parallel agents α/β. Target ship: v0.3.0. | 31 | tech-lead + general-purpose ×2 | 2026-04-24 |
<!-- Plan 30 complete 2026-04-23 — PR #74 squash `7733ae2`, see Recently Done -->
<!-- Plan 29 complete 2026-04-23 — PR #72 squash `9eb2bff`, see Recently Done -->
<!-- Plan 28 complete 2026-04-23 — Phase 2 PR #71 squash `9ce25a4` + Phase 3 PR #70 squash `ee55237`, see Recently Done -->
<!-- Plan 27 complete 2026-04-22 — PR1 squash `0885981` + PR2 squash `719255d`, see Recently Done -->

## Pending

| Task | Plan | Agent | Blocked By |
|------|------|-------|------------|
<!-- Plan 26 candidates (website concept-page rewrite, skills frontmatter convention) filed in Backlog P2 Group C — pick up as next sweep -->

## Recently Done

| Task | Plan | Agent | Date |
|------|------|-------|------|
| Plan 30 — guide viewer perf + list/catalog/guide polish bundle (PR #74 squash `7733ae2`). **User report:** `bonsai guide` tab-switching slow — every tab-first-visit paid fresh `glamour.NewTermRenderer(WithAutoStyle())` cost (termenv `HasDarkBackground()` OSC 11 query + goldmark/chroma init, ~100-200ms each, confirmed from glamour source at `~/go/pkg/mod/github.com/charmbracelet/glamour@v1.0.0/glamour.go:70-99+306-322`). 4 tabs × first-visit = 4 constructions per session. **Fix (Phase A):** (A1) `ViewerStage` gains `renderers map[int]*glamour.TermRenderer` cache keyed by viewport width — `rendererFor(width)` returns cached or builds-and-caches; `render.go` split `renderMarkdown` (one-shot, kept for tests) + new `renderMarkdownWith(content, renderer)`; `refreshViewportContent` swaps to cached path. Per-width construction now O(widths) not O(tabs × widths) — fixed-size terminal = 1 construction for whole session. (A2) Pre-warm via `tea.Cmd` dispatched on first `WindowSizeMsg` per distinct width (tracked via new `preWarmedWidth int` field); cmd builds a **local** renderer on goroutine (no shared-state mutation), renders all topics, returns `preWarmMsg{width, results map[int]string}`; `Update` writes results to `s.rendered` on tea loop — race-free by construction. (A3) 4 new regression tests + 1 width-change test added post-review: `TestViewer_RendererCachedPerWidth`, `TestViewer_RendererPerDistinctWidth`, `TestViewer_PreWarmPopulatesCache`, `TestViewer_TabSwitchUsesCachedRenderer`, `TestViewer_WidthChangeDispatchesPreWarm`. **Bundled NIT cleanups (Phase B-D) — pairable "batch one-shot" backlog items:** (B1) `internal/tui/guideflow/viewer.go:243` `const chromeRows = 8` magic replaced with `headerH + footerH + 4` via new exported `Stage.ChromeHeights(keys []KeyHint) (headerH, footerH int)` method on `initflow/stage.go` (mirrors `renderFrame`'s own composition math). (B2) `labelFor`/`shortFor` default fallback unified via new private `deriveLabel(key)` helper in `guideflow.go` — both derive from hyphen→space uppercase, `shortFor` truncates to 5. No behaviour change for 4 known keys. (B3) `NewStage(0, StageLabel{English:"GUIDE"}, "GUIDE", ...)` trimmed to `StageLabel{}` + `""` title since `SetHeaderAction("GUIDE")` supersedes them. (C1) Dedupe local `itoa` — dropped from `listflow/agent_panel.go` + `initflow/enso.go`; all 4 call sites (`agent_panel.go:219`, `initflow/chrome.go:202-203`, `initflow/enso.go:151`) swapped to `strconv.Itoa`. "Avoid strconv import" rationale was thin. (C2) New `TestRenderWorkspaceBlock_EmptyString` locks empty-string → "Workspace missing" hint contract. (C3) New `TestRunList_NoAgents` — zero-agent config still renders "LIST" header + "0 agents" count without panic. (D1) `catalogflow/browser.go` `timeZero()` wrapper deleted, inlined `time.Time{}` at line 126 (`time` import kept — type still needed). (D2) `category` struct docstring rewritten — stale `filterHidZero` reference was for a field that never existed. (D3) Redundant `expanded: false` dropped from `browser.go:139` + `initflow/branches.go:234` (zero value). (D4) Agents section `buildCategories` entries gain `Meta: map[string]string{"Kind": "agent"}` — details block now renders `KIND: agent` instead of `(no extra metadata)`. (D5) New `TestBrowser_ZeroCountTabRendersMuted` forces truecolor profile via `termenv.SetDefaultOutput`, asserts muted styling around `(0)` suffix. (D6) Dead-code sweep via `go vet` + `deadcode` tool — catalogflow clean, every package-level func still referenced. **Process:** dispatched via `isolation: worktree` off `d49a08e` (plan spec commit); agent returned 4-commit draft PR with `make build && go test ./...` green first pass. CI lint failed on trailing blank lines at `initflow/enso.go:247` + `listflow/agent_panel.go:364` — leftover from `itoa` func removal; tech-lead ran `gofmt -w` in worktree + pushed `e3e76fa`. All 6 CI checks green post-fix (test/lint/Analyze-Go/govulncheck/CodeQL/GitGuardian). Independent code-review agent returned **PASS-WITH-MINORS** — 3 non-blocking: (1) stale `renderMarkdown` doc comment claimed usage by `cmd/guide.go renderStatic` which has its own inline renderer, (2) unnecessary `maxInt` helper in new `browser_test.go` (Go 1.25 has builtin `max`), (3) missing width-change dispatch test. Fix-agent shipped `f28dd7b` addressing all 3 (subtle: test needed widths 120→84-clamp vs 80→76-clamp distinction to actually fire the distinct-width branch since `PanelWidth` clamps at 84). CI re-run green. Net +624/−86 across 14 files (6 commits). **Post-merge:** worktree-held-branch pattern 13× this month — manual `git worktree remove -f -f .claude/worktrees/agent-a6d462d3` + `git branch -D plan-30-guide-perf-polish worktree-agent-a6d462d3` + `git push origin --delete plan-30-guide-perf-polish`. Resolves user-reported perf regression AND 2 long-standing Plan-28 backlog NIT bundles in single shipping unit. Plan archived. | 30 | general-purpose ×3 + tech-lead | 2026-04-23 |
| Archive-reconcile sweep — commit `45df4cd` (21 files, 15+/15−). Moved 20 shipped plans Plans/Active/ → Plans/Archive/ via `git mv` with proper rename detection (98-100% similarity); synced 14 stale Status frontmatters to `Complete — shipped YYYY-MM-DD (PR #N)` drawing source-of-truth from StatusArchive.md rows (01=04-15, 02=04-15, 03=04-16, 05=04-16 #7, 08=04-16/04-21 phase-split, 09=04-16, 10=04-17, 11=04-17, 12=04-17 #20, 13=04-17 #22, 14=04-17 bundle #24, 17=04-17 #24, 21=04-21 #46, 29=04-23 #72). 6 plans (04/06/07/16/18/20) already declared Complete — pure renames. Plans/Active/ now empty. Backlog P1 bookkeeping item "Re-archive Plan 29 file" closed via sweep. **Gotcha captured in memory Notes:** `git commit -o <paths>` with only Archive-side of a rename breaks rename detection — recovered via `git reset --soft HEAD~1` + plain `git commit`. | — | tech-lead | 2026-04-23 |
| Plan 29 — init + add bug bundle (PR #72 squash `9eb2bff`). 8 phases across 8 bugs + 4 test gaps. Ships: (A) `cmd/add.go:77-85` — `HeaderAction: "ADD"` + `HeaderRightLabel: "GRAFTING INTO"` (was stale `"INIT"` + `"PLANTING INTO"` inherited verbatim from init_flow.go); stale comment block at 72-76 dropped. (B1) `internal/tui/addflow/conflicts.go:View` — dead `w := s.Width()` + 80-col fallback dropped; only `h` needed. (B2) Same file `listHeight()` — `const fixedRows = 15` → `14` to match actual non-list row count (intro 3 + 11 rhythm rows); comment + const reconciled; list gets 1 additional row on 20-row terminals. (C1) `internal/tui/addflow/grow.go:32` — redundant `SetRailHidden(true)` dropped (bodyOnly branch bypasses renderFrame entirely, rail flag unreachable); comment updated. (D1) `internal/tui/addflow/yield.go:26-27` — stale "rail position 5" doc → "rail position 3 (StageIdxYield)" after Plan 27 shrunk rail to 4. (D2) `renderSuccess` — collapsed duplicate `totalAbilitiesStat`/`totalAbilities` computations into single `totalAbilities int` used by both hero-stats line + summary row. (E1) `internal/tui/addflow/ground.go:33` — dropped dead `_ lipgloss.Position // reserved` field (gofmt realigned adjacent struct comment columns — documented deviation). (F) 4 new tests: `TestConflicts_ViewportFollowsFocus` (10 files + h=20 + focus=8 → `shortName(s.files[8])` in render, `shortName(s.files[0])` NOT), `TestConflicts_ColorTonesDifferPerAction` (Keep != Overwrite rendered-string smoke), `TestConflicts_LowercaseKMovesFocus` (lowercase `k` is focus-up, locks contract), `TestGenerateStage_BodyOnlyDropsChrome` (bodyOnly=true output has no rail glyphs or `BONSAI 一` footer). (H1) `internal/tui/addflow/ground.go` — new `invalidWorkspaceReason(ws string) string` helper rejects absolute paths (`filepath.IsAbs`) + path-escape segments (`..` after `filepath.Clean`) with 4-line security rationale block; wired into `Update`'s Enter branch AFTER `NormaliseWorkspace`, BEFORE `existingWorkspaces` duplicate-check. (H2) `internal/tui/initflow/vessel.go:validate()` — mirrored check on STATION input; `path/filepath` added. (H3) 5 new validator tests: `TestGround_RejectsAbsolutePath`, `TestGround_RejectsParentEscape`, `TestGround_RejectsHiddenParentEscape` (`nested/../..`), `TestVessel_RejectsAbsoluteStation`, `TestVessel_RejectsParentEscapeStation`. `TestNormaliseWorkspace` fixtures untouched + still passing (no over-rejection). Net +259/−34 across 10 files (7 phase-scoped commits). Process: dispatched single general-purpose agent via `isolation: worktree` off `87062c4` (plan spec commit); agent returned draft PR clean w/ 2 documented deviations (gofmt struct-comment realignment + `shortName` test-file-local helper). Independent code-review agent returned **PASS** (3 nits + 2 test-gaps, all filed to Backlog as Plan-29-cosmetic + Plan-29-test-gap). Independent security-review agent returned **PASS** on Phase H (0 blocking, 4 minor hardening ideas filed as Plan-29-security-hardening). 6/6 CI green (test/lint/Analyze-Go/govulncheck/CodeQL/GitGuardian). Post-merge: worktree-held-branch pattern hit 12× this month — manual `git worktree remove -f -f .claude/worktrees/agent-a208a3fa` + `git branch -D plan-29/init-add-bug-bundle worktree-agent-a208a3fa` + `git push origin --delete plan-29/init-add-bug-bundle`. Plan archived. Dispatch pattern proven again: deep-dive audit → plan spec with phase-by-phase file:line targets → single dispatch → parallel code+security reviews → same-session merge, zero blocker findings. Parallel-session on list/catalog/guide (Plan 28 Phase 2+3) ran start-to-finish concurrently w/ zero collision — scope-locked plan kept agent away from `cmd/guide.go` / `cmd/catalog.go` / `cmd/list.go` / `internal/tui/{listflow,guideflow,catalogflow}/`. | 29 | general-purpose + tech-lead | 2026-04-23 |
| Plan 28 **complete** — Phase 2 `bonsai list` cinematic (PR #71 squash `9ce25a4`) + Phase 3 `bonsai guide` cinematic (PR #70 squash `ee55237`) shipped in parallel off `f8f90de`. Phase 2 ships: (A) new `internal/tui/listflow/` package — `RenderAll(cfg, cat, version, projectDir, termW, termH) string` pure static string builder; `RenderAgentPanel` stacks `tui.TitledPanel` + workspace `tui.FileTree` (cap 50 entries + muted `... (N more)` hint on overflow per decision D2); chromeless header via `initflow.RenderHeader(version, projectDir, "LIST", "", width, safe)` (empty rightLabel collapses row 1); min-size floor via `TerminalTooSmall`+`RenderMinSizeFloor`. (B) Security — workspace-path escape refused via `filepath.Rel`-based ancestor check (review minor M1 dropped buggy `strings.Contains("..")` false-positive); symlink-loop defense via `EvalSymlinks` + target-escape ancestor check (review minor M4 stripped dead `d.IsDir()` branches + documented that `filepath.WalkDir` doesn't follow symlinks by default). (C) Workspace-missing CTA: `tui.Hint("Workspace missing — run: bonsai update")` per decision D3. (D) Scaffolding row + counts footer preserved. (E) `cmd/list.go` rewired to single `fmt.Print(listflow.RenderAll(...))` call. 14 unit tests + 1 e2e. Phase 3 ships: (A) new `internal/tui/guideflow/` package — `Topic` struct (no kanji per D1); `NewTopics(contents)` canonical-order helper; `renderMarkdown(content, width)` + `stripFrontmatter` in `render.go`; `ViewerStage` BubbleTea model embedding `*initflow.Stage` with `SetRailHidden(true)`, 4-tab strip (`QUICKSTART · CONCEPTS · CLI · CUSTOM` full / `START · CONCP · CLI · CUSTM` short when measured full strip exceeds `initflow.PanelWidth(w)` — review minor M2 dropped 60-floor), `bubbles/viewport` scroll body, glamour cache keyed `idx:width`, keys `tab/shift+tab/←/→/h/l` cycle + `↑↓/j/k/pgup/pgdn/f/b` scroll + `g/home` top + `G/end` bottom + `q/esc/ctrl+c` quit. (B) `cmd/guide.go` rewrite — TTY path → viewer via `tea.NewProgram(stage, tea.WithAltScreen()).Run()` wrapped `"guide viewer: %w"`; arg + non-TTY → `renderStatic` preserves pre-Plan-28 glamour; no-arg + non-TTY → `errors.New(noTTYNoArgErr)` with EXACT D4 string `bonsai guide: specify a topic when piping output (quickstart, concepts, cli, custom-files)` exit code 1 (M3 swapped awkward `fmt.Errorf("%s", ...)`); redundant `TerminalTooSmall` check in `View()` dropped (M4 — `Stage.RenderFrame` already short-circuits). Huh picker deleted. 23 unit tests + 5 cmd tests. Process: dispatched both agents in parallel off `f8f90de`; Phase 3 returned +1042/−31 w/ lint failure (unused `guideTopic` struct + `guideTopics` var leftover from pre-rewire — fixed via direct worktree edit commit `39fc711`), Phase 2 returned +1125/−150 clean. Independent code-review agents both returned **PASS-WITH-MINORS** (zero blockers); Phase 2 review flagged 4 correctness minors + 2 stale comments + TestMain-scope side-effect + 5 test gaps; Phase 3 review flagged 4 cosmetic/correctness minors + 1 test gap. Dispatched 2 fix-agents in parallel; Phase 2 returned `80baab5` (+130/−58 across 4 files — dropped buggy substring guard, removed unused `termW` param, deleted `statDir` wrapper with false docstring, stripped dead `d.IsDir()` branches, 2 stale comment strips, moved `TestMain` catalog wiring into scoped helper + 3 new tests for absolute-path escape, `node_modules`/`.git` skip, legitimate `my..workspace` regression); Phase 3 returned `4527653` (+89/−32 across 3 files — dropped unused `width` param, fixed narrow-width short-label threshold to measure against `PanelWidth(w)`, `errors.New` swap, removed redundant min-size pre-check, new `TestViewer_ScrollKeyDelegation` covering `j/k/f/b` → `viewport.YOffset`). Both PRs all 6 CI checks green (test/lint/Analyze-Go/govulncheck/CodeQL/GitGuardian). Merged PR #70 first (`ee55237`), parallel session landed `87062c4` (plan-29 spec) mid-way, PR #71 rebased trivially clean (disjoint files), merged (`9ce25a4`). Cross-phase verification: `make build && go test ./...` green; `bonsai --help` shows 8 commands (no `completion`); `bonsai completion zsh \| head` still functional; `bonsai list \| head` renders cinematic chrome + scaffolding row + agent panel + `station/` file tree + counts footer; `bonsai catalog \| head` renders all 7 static sections (non-TTY fallback); `bonsai guide </dev/null` exit 1 + EXACT D4 error. Worktree-held-branch pattern hit 10× + 11× this month — manual `git worktree remove -f -f` + `git branch -D` + `git push origin --delete` both cycles. Plan archived. | 28 | general-purpose ×4 + tech-lead | 2026-04-23 |
| Plan 27 PR2 — Phase C polish + Phase D verification (PR #69 squash `719255d`). Ships: (C1) `internal/tui/addflow/conflicts.go` chromeless full-screen surface — `Chromeless() bool { return true }`; no `RenderFrame`/`RenderHeader`/`RenderFooter`/`RenderEnsoRail` calls; dedicated 衝 · CONFLICT title row + inline CTA. (C2) Vertical list replaces horizontal tab strip — one row per file; `↑↓/jk` move focus (no wrap); `1/2/3` set action Keep/Overwrite/Backup; `␣` cycles Keep→Overwrite→Backup→Keep; `↵` advances. `initflow.Viewport` wraps when rows exceed height. (C3) Per-file action color — each row's text + glyph color keyed off `s.action[f.RelPath]` via `ConflictRowStyle(toneFor(action))` + `ConflictActionGlyph`; updates live. (C4) Batch-resolve row — `K/O/B` uppercase apply to all rows; lowercase `k/o/b` no-op (asserted in `TestConflicts_LowercaseBatchKeysNoOp`); rendered below list with `batch resolve:` caption. (C5) Conflict rail drop verified — no residual rail in chromeless body. (C6/C7) Ground + Grow chromeless — `Chromeless() bool { return true }` on both; Grow uses new `SetBodyOnly(true)` additive flag on `internal/tui/initflow/generate.go` (zero-value-safe, init's Planting unaffected); inline key hints in body. (C8) `stripAgentSuffix` helper in `select.go` — case-insensitive strip of trailing "Agent"/"AGENT"/" Agent" at render time (stored `DisplayName` unchanged); full description renders (truncation only on row overflow); `(installed)` badge pad-fill right-aligned in rightmost column AFTER description. (C9) Yield success variant Chromeless — new `heroStats` line ("N abilities wired · agent · lock synced") matches Planted's rhythm; inline key hints (`↵ exit · q quit`). Test updates: `conflicts_test.go` rewritten (fields `catIdx/radio`→`focus/action`); new `TestConflicts_FocusMoveClamps`/`DigitKeysSetAction`/`SpaceCyclesAction`/`BatchKeyAppliesToAll`/`LowercaseBatchKeysNoOp`/`Chromeless`/`ColorFollowsAction`. `select_test.go` adds `TestSelect_StripsAgentSuffix` + table `TestStripAgentSuffix_Cases` (empty / standalone "Agent" / "MultiAgent" / whitespace-wrapped / case variants). Net +680/−359 across 8 files (6 addflow + 1 initflow additive + 1 select_test). Process: dispatched via `isolation: worktree` off origin/main at `5d7c57e` (post Plan 28 Phase 1 + Plan 27 PR1 ships); rebased once when Plan 28 `1b0b50a` + Plan 27 PR1 `0885981` landed mid-work — clean auto-merge. Independent code-review PASS with minors — 4 minor / 3 nit / 3 test-gap, all cosmetic/low-value (filed to Backlog Group B as [Plan-27-cosmetic]); 5/5 CI green (test/lint/Analyze-Go/govulncheck/CodeQL). Worktree-held-branch pattern hit 9× — manual `git worktree remove -f -f` + `git branch -D` + `git push origin --delete`. Plan 27 archived. Plan 28 `RenderHeader` signature extension already integrated via parallel session rebase — no coordination friction. | 27 | general-purpose + tech-lead | 2026-04-22 |
| Plan 28 Phase 1 — cinematic `bonsai catalog` + RenderHeader extension + hide `completion` (PR #68 squash `1b0b50a` + fix-agent commit `64119e5` + rebase `c1fea7e` on top of Plan 27 PR1). Ships: (A) new `internal/tui/catalogflow/` package — `BrowserStage` BubbleTea model embedding `initflow.Stage` (rail hidden); 7 tabs across all catalog sections (Agents/Skills/Workflows/Protocols/Sensors/Routines/Scaffolding); keys `← → / h l` tab wraps, `↑ ↓ / j k` focus clamp, `?` inline-expand, `q / esc / ctrl-c / enter` quit via `tea.Quit`; `-a <agent>` filter greys empty tabs `(0)` suffix but keeps them in the strip; narrow-width tab strip (<96 cols) switches to 5-char short labels (`AGENT`/`SKILL`/`FLOWS`/`PROTO`/`SENSE`/`RTNES`/`SCAFF`) to fit 70-col min-size floor. (B) `initflow.RenderHeader` signature extended — `(version, projectDir, action, rightLabel string, width int, safe bool)`; empty `rightLabel` collapses right-block row 1 for commands without a "destination" label; `Stage` gains `headerAction` + `headerRightLabel` fields w/ `ApplyContextHeader(ctx)` setter; `StageContext` gains `HeaderAction` + `HeaderRightLabel` fields. (C) every existing stage ctor (init + add) calls `base.ApplyContextHeader(ctx)` — 12 trivial 1-line additions; existing behavior preserved via `cmd/init_flow.go` + `cmd/add.go` stamping `HeaderAction: "INIT"` + `HeaderRightLabel: "PLANTING INTO"` on StageContext. (D) `cmd/catalog.go` rewired — factored original body into `renderCatalogStatic(cat, agentFilter)`; TTY invocations launch `catalogflow.NewBrowser(cat, agentFilter, projectDir)` via `tea.NewProgram(stage, tea.WithAltScreen()).Run()` with error wrap `"catalog browser: %w"`; non-TTY falls back to static helper (piped output identical to prior behavior). (E) `cmd/root.go` adds `rootCmd.CompletionOptions.HiddenDefaultCmd = true` in init block — `bonsai --help` no longer lists `completion` but `bonsai completion zsh > ~/.zsh/completions/_bonsai` still functional. Net +1322/−46 across 25 files (3 commits pre-review + 1 fix-agent commit + 1 rebase). Process: independent code-review PASS-WITH-MINORS (0 major / 4 minor / 6 nit); fix-agent addressed all 4 minors in commit `64119e5` (projectDir threading via `NewStage`, short-label tab strip under 96 cols + regression tests, routed View() through `Stage.RenderFrame(body, keys)` consuming the stored header setters (path A), `tea.Run` error wrap). 6/6 CI green post-rebase (test/lint/Analyze-Go/govulncheck/CodeQL/GitGuardian). **Mid-work coordination:** accidentally shipped Plan 27's pre-staged `graft.go → branches.go` rename in `d9ddee4` (parallel-session WIP leak via shared git index — `git commit` grabbed pre-staged renames despite path-restricted `git add`); confirmed pure 100%-similarity rename, no callers broken, left in place. Plan 27 PR1 (`0885981`) landed mid-Phase-1 causing PR #68 to go `DIRTY`; rebased onto origin/main, resolved single conflict in `internal/tui/addflow/ground.go` (both sides needed — `ApplyContextHeader(ctx)` + `SetRailLabels(StageLabels)` per peer-stage order). Post-merge: worktree-held-branch pattern 8× — manual `git worktree remove -f -f` + `git branch -D` + `git push origin --delete`. 6 NITs filed to Backlog Group B (Plan-28-cosmetic tag). | 28 | general-purpose ×2 + tech-lead | 2026-04-22 |
| Plan 27 PR1 — Phase A foundations + Phase B bug fixes (PR #67 squash `0885981` + fix-agent commit `fab4cdc`). Ships: (A1) `StageLabels` shrunk from 7→4 visible stages (選 SELECT · 枝 BRANCHES · 観 OBSERVE · 結 YIELD) via `internal/tui/addflow/addflow.go`; added `StageIdxOffRail = -1` sentinel; `internal/tui/initflow/stage.go:renderFrame` gated rail render on non-negative index; Ground/Grow/Conflicts stages render off-rail via the new sentinel (chromeless body polish deferred to PR2 Phase C). (A2) `graft.go`→`branches.go` + `graft_test.go`→`branches_test.go` via `git mv`; `GraftStage`/`GraftContext`/`GraftResult`/`NewNewAgentGraft`/`NewAddItemsGraft`/`StageIdxGraft` all renamed to `Branches*` equivalents; fix-agent follow-up also renamed test fns + helpers (`TestGraft*`→`TestBranches*`, `newTestGraft*`→`newTestBranches*`, `graftPressKey`→`branchesPressKey`, etc). (A3) `internal/tui/addflow/observe.go` dropped the `observePanelW = 60` cap; Agent + Abilities blocks now use `initflow.PanelWidth(s.Width())` to match Select/Branches/Yield. (B1) Stale Select-state bug (esc-back + re-pick agent showed prev agent's abilities) fixed via harness re-splice: `LazyGroup.Reset()` clears spliced flag in `internal/tui/harness/steps.go`; `spliceRecord{lg, insertAt, length}` tracked in `Harness.splices` + new `unspliceAbove()` unwinds records at/above cursor on esc-back (reverse iteration to keep offsets stable; non-empty guard per fix-agent defensive hardening); regression tests `TestLazyGroupResplicesOnEscBack` + multi-splice + empty-children variants. (B2) Cross-agent conflict leak (adding frontend flagged tech-lead files as conflicts) fixed via 3 new `*ForAgent` variants — `PathScopedRulesForAgent`, `WorkflowSkillsForAgent`, `SettingsJSONForAgent` scoped to single `*config.InstalledAgent`; legacy all-agents variants preserved for `bonsai update`; `cmd/add.go` call sites in both new-agent + add-items branches swapped; scope regression tests for all 3 variants with legacy-path negative controls. Net +1242/-348 across 15 files (incl. fix-agent +136/-6). Process: dispatched via `isolation: worktree`, rebased once on `d9ddee4` (parallel Plan 28 session's rename pre-step on same files — zero-content overlap, clean auto-merge); independent code-review PASS with minors (test rename hygiene + defensive guard + expanded coverage — all addressed in fix-agent commit) + security-review PASS zero findings (2 advisories filed to Backlog). 6/6 CI green both rounds (test/lint/Analyze-Go/govulncheck/CodeQL/GitGuardian). Worktree-held-branch pattern hit 7× — manual `git worktree remove -f -f` + `git branch -D` + `git push origin --delete`. PR2 (Phase C polish + D verification) dispatches next. | 27 | general-purpose ×2 + tech-lead | 2026-04-22 |
| Plan 26 — P2 knock-off bundle (PR #66 squash `a9df552`). 4 independent cleanups: (1) `station/agent/Sensors/context-guard.sh` planning-reminder paths now use `docs_path` var (adds missing `station/` prefix — matches wrap-up checklist pattern at lines 122-124); (2) `station/agent/Core/routines.md` drops 2 stale-output blank lines splitting Dashboard + Definitions tables (generator already correct per `internal/generate/generate.go:917-1061`, file was stale output of older generator); (3) `cmd/root.go:173` + `cmd/add.go:329` replace `filtered := slice[:0]` aliasing w/ `make([]string, 0, len(slice)-len(dropped))` pre-allocation (2 sites — anti-pattern, safe today but silent-break if iteration order flips); (4) `cmd/add.go` rename inner closures `installedSet` → `installedItems` at lines 532 + 619 + all 6 internal call sites (file-scope `func installedSet(cfg)` at 349 + external caller at 119 preserved); (5) new `TestRoutineDashboardNoBlankRows` regression (+137L) — catalog w/ 7 tech-lead-default routines at mixed 5/7/14d frequencies, asserts no blank row splits body rows between `ROUTINE_DASHBOARD_START`/`END` markers OR in Definitions body; tolerates generator's expected blanks flanking markers. Net +149/−14 across 5 files. Independent code review PASS zero findings (no major/minor/nit); 6/6 CI green (test/lint/Analyze-Go/govulncheck/CodeQL/GitGuardian). Post-merge: worktree-held-branch pattern hit again → manual `git worktree remove -f -f` + `git branch -D` + `git push origin --delete`. | 26 | general-purpose + tech-lead | 2026-04-22 |
| **v0.2.0 release ship** — pre-release docs audit bundle PR #65 squash `e9b7cba` + tag `v0.2.0` + Homebrew tap formula update `29fd0da`. Bundle: (A) sensor count 12→13 everywhere (README line 158, website overview+sensors+catalog.json via `website/scripts/generate-catalog.mjs` re-run); (B) tagline mechanical replace "structured language" → "workspace" in `index.mdx` (3 spots), `astro.config.mjs`, `why-bonsai.mdx`; (C) invalid agent types stripped — `review-checklist/meta.yaml` drops `reviewer`, `test-strategy/meta.yaml` drops `qa`; (D) LICENSE 2025 → 2025-2026; (E) Go 1.24+ → 1.25+ in README + CONTRIBUTING (matches go.mod 1.25.0); (F) `station/CLAUDE.md.bak` deleted (gitignored Plan 01 leftover); (G) `Plans/Archive/23-uiux-phase2-add.md` frontmatter `Status: Draft` → `Complete`; (H) `station/code-index.md` refreshed — drops deleted `cmd/add_redesign.go`, repoints `init_redesign.go` → `init_flow.go`, line-drift fixes across catalog/generate/config/styles/prompts/root, adds new tables for `internal/tui/harness/` + `initflow/` + `addflow/`; (I) `docs/README.md` rewritten to clarify embedded-binary vs Starlight-website split; (J) `CHANGELOG.md` `[Unreleased]` cut to `[0.2.0] - 2026-04-22` with curated entries grouped Added/Changed/Removed/Fixed/Security covering Plans 19-25 + statusLine + push-CI widening + binary path fix + Go bump; v0.1.0–v0.1.3 dates backfilled `2026-04-15` → `2026-04-16` to match git tag creator dates; link refs rewired. 15 files, +224/−96. Process: self-executed P0+P1 as mechanical edits in single commit `1be622a`, pushed branch, dispatched general-purpose agent for P2 + CHANGELOG (3 commits `ff24fce` + `c7d9442` + `c05fca4`); PR #65 opened, 8/8 CI green (test/build/lint/govulncheck/CodeQL/Analyze-Go/GitGuardian, deploy-skip push-only); squash-merged. Parallel session pushed 2 unrelated commits (`d5a7dc9` + `d25eef1`) mid-work to main — non-overlapping files, clean merge forward. Tag pushed; GoReleaser built+uploaded 6 platform binaries + checksums to GitHub Release; **Homebrew step failed at 401 Bad credentials** — `HOMEBREW_TAP_TOKEN` PAT expired. User rotated PAT + set secret; formula for v0.2.0 pushed manually via `gh api PUT /repos/LastStep/homebrew-tap/contents/Formula/bonsai.rb` with SHAs from `checksums.txt` (commit `29fd0da` on tap). Live verification: formula at v0.2.0 with all 4 platform SHAs correct. Deferred to Plan 26: full website concept-page rewrite (heavier framing pivot beyond tagline), skills frontmatter convention decision. **All pre-OSS-launch gates cleared.** | 26 | tech-lead + general-purpose | 2026-04-22 |
| Plan 23 **complete** — Phase 3 cutover + 7 bundled backlog cleanups (PR #64 squash `788fa6c` + post-review fix `44e6874`). Ships: (A) flip default — `BONSAI_ADD_REDESIGN` env gate deleted, cinematic `runAdd` body lives in `cmd/add.go`; (B) legacy delete — `runAdd` legacy body, `runAddSpinner`, `buildNewAgentSteps`, `buildAddItemsSteps`, `addOutcome` struct, `normaliseWorkspace`, `workspaceUniqueValidator`, `newDescriber`, `userSensorOptions`, Phase-1-deferred `NewYieldAddItemsDeferred` + `yieldModeAddItemsDeferred` + `renderAddItemsDeferred()` all gone (zero grep hits); (C) `cmd/add_redesign.go` deleted, `cmd/init_redesign.go` → `cmd/init_flow.go` via `git mv` (96% similarity rename); (D) bundled cleanups — dead post-harness Generate-error warning deleted from BOTH `cmd/init_flow.go` AND `cmd/add.go` (latter caught by code review), three harness composition tests for `NewConditional(NewLazy(...))`, `growSucceeded` predicate closure capture, new `addflow.NewYieldUnknownAgent` variant with `bonsai update` CTA, `.bak` write-error silent-discard fixed in BOTH helpers (drops failed-backup paths + collected warning), conflicts post-harness type-scan instead of `len(results)-2` arithmetic, `cmd/add_test.go` new (368L, 9 table tests with real `chmod 0500` perms). Net +947/−990 across 9 files. Post-merge cleanup: `gh pr merge --delete-branch` left remote branch behind (worktree-checked-out gotcha 6×). Plan archived. | 23 | general-purpose ×2 + tech-lead | 2026-04-22 |
| Plan 23 Phase 2 — `bonsai add` cinematic add-items branch + `ConflictsStage` (PR #62 squash `61777c0`). Ships: (A) splicer wires real add-items sub-sequence `[AddItemsGraft, Observe]` replacing Phase 1's terminal splice; (B) `buildAddGrowAction` type-scans `GraftResult` from prev; (C) `internal/tui/addflow/conflicts.go` (438L new) — chromeless tabbed stage, per-file 3-way radio (Keep/Overwrite/Backup); (D) rail extended 6→7 with `衝 しょう CONFLICT`; (E) `initflow.ConflictRowStyle(tone)` real body; (F) `ConflictAction` enum in `internal/config/lockfile.go` (in-memory only); (G) `applyCinematicConflictPicks` post-harness helper; (H) Observe `SetPrior` type-scan refactor; (I) 12 new tests. Independent code review PASS + security review MINOR_FINDINGS (3 carry-forward items). | 23 | general-purpose + tech-lead | 2026-04-22 |
| Plan 25 — README revamp (PR #61 squash `d6785e7`). Audience-first rewrite: new tagline "A workspace for your coding agent" (from "A structured language for working with AI agents"), status-early-stage shield badge, new `## Who Bonsai is for` section (solo devs + small teams, mechanism-over-personality bullets, `station/` tree snippet, CLAUDE.md rebuttal), merged `## Why Bonsai` + `## What's Inside` into single `## How it works`, folded Documentation + Contributing into footer nav, cut AI-readme smells (`by design`, `powerful out of the box`, `One binary. No runtime.`, `teammates, not tools`, `structured language` framing). Process: spawned independent general-purpose research agent pre-draft for critical review; all high-impact findings adopted. | 25 | tech-lead | 2026-04-22 |
| Plan 23 Phase 1 — `bonsai add` cinematic new-agent path (PR #59 squash `163bc45`). Ships: (A) new `internal/tui/addflow/` package mirroring initflow shape — 6-stage cinematic flow (選 Select · 地 Ground · 接 Graft · 観 Observe · 育 Grow · 結 Yield); (B) initflow additive exports; (C) `cmd/add_redesign.go` 311L harness wiring behind `BONSAI_ADD_REDESIGN=1` env gate; (D) 46 unit tests. Initial PR had lint FAIL + cross-track regressions; rebased on origin/main, fixed B2 gofmt + B1 silent-misroute bug via `NewYieldAddItemsDeferred`. | 23 | general-purpose + tech-lead | 2026-04-22 |
| Pre-launch security sweep — (commits `ef55f4f` + hotfix `7c1dd49`, direct-to-main): Dependabot alert #1 (Astro XSS CVE-2026-41067) auto-fixed by lockfile resolving `astro@6.1.7`; 2 low-severity CodeQL `go/useless-assignment-to-local` alerts silenced (`internal/tui/styles.go:211` + `cmd/add.go:690`); secret scanning clean. Widened `ci.yml` to trigger on `push: branches: [main]` in addition to `pull_request`. First push-CI run caught follow-on breakage in `ef55f4f` (staged Plan 23 WIP hunks alongside intended changes) — hotfix `7c1dd49` removed WIP, Plan 23 WIP restored to working tree for parallel session. Net lesson: push-CI paid for itself within 5 min. | — | tech-lead | 2026-04-22 |
| Plan 24 — pre-launch polish bundle (PR #58 squash `4ef8271`). Ships: (A) `CHANGELOG.md` keep-a-changelog 1.1.0 curated v0.1.0–v0.1.3 backfill + `[Unreleased]` stub; (B) `.github/workflows/docs.yml` adds `pull_request` trigger + guards (broken MDX now fails at PR time); (C) root `Bonsai/CLAUDE.md` `internal/tui/` tree refresh; (D) Backlog Group C/D consolidation; (E) 5 `good first issue` + `help wanted` issues filed (#53/#54/#55/#56/#57). CI gotcha: pre-existing gofmt drift in `observe.go:466` + `planted.go:423` caught on PR lint; fixed before merge. | 24 | general-purpose | 2026-04-22 |
| statusLine redesign (project scope) — new `station/agent/Sensors/statusline.sh` persistent bar: `cave:<mode> · <workspace> · <model> · <branch>[*<dirty>] · ctx N% · 5h N% · 7d N% · <elapsed> · $<cost>` with sage/sand/rose 256-color tiers + `NO_COLOR` + `BONSAI_STATUSLINE_HIDE` toggles. Caveman badge wrapped natively (reads `$CLAUDE_CONFIG_DIR/.caveman-active` with symlink refuse + 64B cap + whitelist). Wired via top-level `statusLine` stanza in project `.claude/settings.json`. Rewrote `status-bar.sh` → warnings-only; rewrote `context-guard.sh` → self-sufficient (no `/tmp` state file dep). Direct-to-main per fast-iter UX convention. Catalog port filed as Backlog P2 Group E (Phase 2). | — | tech-lead | 2026-04-22 |

> Done items older than 2026-04-22 moved to [StatusArchive.md](StatusArchive.md).
