---
tags: [log, session, plan-11, ui-ux]
description: UI/UX overhaul Phase 1 — audit, patches, implementation, merge.
---

# 2026-04-17 — UI/UX Overhaul Phase 1

**Plan:** [11-uiux-overhaul-phase1.md](../Playbook/Plans/Active/11-uiux-overhaul-phase1.md)
**PR:** [#14](https://github.com/LastStep/Bonsai/pull/14) — merged as `effcfd8`
**Agent:** tech-lead (orchestrator) + general-purpose (implementer) + general-purpose (reviewer)

## Flow

Audit → patch → dispatch → self-review → independent review → final audit → merge.

## Plan patches applied pre-implementation

1. **Step 1B focused-border dark value preserved** — plan originally replaced `#3A5F4A` (muted green) with the brighter `Leaf` palette token, contradicting its own "zero dark-terminal regression" success criterion. Patched to inline `AdaptiveColor{Dark: "#3A5F4A", Light: "#5A7F6A"}`.
2. **Spacing contract clarified** — contract now explicit: "commands own exactly one trailing `tui.Blank()` **on success paths only**; cancelled paths add nothing."
3. **Verification item added** — `go mod tidy` check that only `termenv` and `go-isatty` move from indirect to direct in `go.mod`.
4. **Design-guide scope follow-up logged in Backlog** — plan makes the catalog `design-guide` skill Bonsai-specific. Backlog item added to later split into generic catalog skill + Bonsai-local `station/` override.

## What shipped

- 9-color palette converted to `lipgloss.AdaptiveColor` (dark values untouched, new light values tuned for contrast)
- `NO_COLOR` / non-TTY / `TERM=dumb` auto-detection in `internal/tui/styles.go:init()`
- `--no-color` flag wired on root command via `PersistentPreRun`
- `Banner(version)` — shows `v{version}` in subtitle when not empty and not `"dev"`
- Spacing normalized across `init`, `add`, `remove`, `update` — panels own their margins, success paths own one trailing `Blank`
- `FatalPanel(title, detail, hint)` replaces scattered `Error/ErrorPanel + os.Exit(1)` pattern across `cmd/`
- `catalog/skills/design-guide` rewritten from generic CSS content to Bonsai-specific TUI/CLI rules, `agents: [tech-lead, backend, frontend, fullstack]`

## Verification

- `go build ./...`, `go vet ./...`, `go test ./...`, `gofmt -s -l .` all clean on the worktree
- `go mod tidy` idempotent; only expected dep promotion
- Manual ANSI-escape smoke tests under `NO_COLOR=1`, `--no-color`, `TERM=dumb`, piped output — zero ESC sequences
- Independent review returned **APPROVE** — no deviations, no scope creep, no findings

## Decisions

- **Kept `design-guide` Bonsai-specific in the catalog** for dogfooding now; follow-up split deferred to backlog rather than blocking Phase 1.
- **Dark values preserved exactly** across all AdaptiveColor conversions — existing users on dark terminals see zero pixel change.

## Follow-ups

- UI/UX Phase 2 — remaining polish (see backlog / next plan)
- Split `design-guide` into generic catalog skill + Bonsai local override (Backlog, P2)
