---
tags: [log, session, plan-22, phase-3]
date: 2026-04-21
plan: 22
phase: 3
pr: 49
squash: 971ee44
---

# Plan 22 Phase 3 — Vessel + Soil stages + header station-strip

## What shipped

PR #49 `971ee44` — squash-merged to main. 8 files, +1019 / -25.

### New

- `internal/tui/initflow/vessel.go` (278L) — `VesselStage` with 3× `bubbles/textinput` (NAME / DESCRIPTION / STATION) on one page. Tab + down / shift-tab + up cycles focus; shift-tab on first field + esc on first field propagate to harness (enables esc-back). Enter on last field submits after validation; Enter on earlier field advances focus. Validation: Name required (trim-empty inline "required" error in LeafDim), Description optional, Station rejects `"/"` (empty falls through to `station/` default). `Result()` returns `map[string]string{"name":..., "description":..., "station":...}`.
- `internal/tui/initflow/vessel_test.go` (185L) — 9 tests: tab / shift-tab focus, enter-advances-focus, required-empty blocks submit, station `/` rejected, result shape, empty description preserved, station default applied, submit completes stage.
- `internal/tui/initflow/soil.go` (261L) — hand-rolled multi-select (explicitly NOT `bubbles/list` — pre-dispatch decision). `↑↓` / `jk` focus with wrap, `␣` toggles (required items ignore), `↵` advances. Required items pre-selected in ctor. Focused row: `ColorLeaf` left border + 7% leaf-tint background. Glyphs: `◆` selected (Leaf), `◇` unselected (ColorRule2). `REQUIRED` badge right-aligned in Bark. `Result()` returns `[]string` of selected Names in input order.
- `internal/tui/initflow/soil_test.go` (179L) — 7 tests: required pre-selected + cannot toggle off, optional toggle both ways, focus wrap, result order, enter completes, Reset preserves selections.
- `internal/tui/initflow/chrome_test.go` (65L) — 4 tests for the header: `~`-collapse on $HOME prefix, absolute path outside $HOME kept literal, **no `station` substring in rendered output** (safe=true AND safe=false), trailing `/` on project name.

### Modified

- `internal/tui/initflow/chrome.go` — `RenderHeader` signature dropped `stationSubdir` parameter. New shape: `RenderHeader(version, projectDir string, width int, safe bool) string`. Right block now renders `parent` (muted) + `projectName` (Bark) + `/` (muted). No `station` segment.
- `internal/tui/initflow/stage.go` — `renderFrame`'s `RenderHeader` call site updated to the 4-arg signature. `Stage.stationDir` field + `StageContext.StationDir` kept (Phase 5 Planted body will render the generated file tree rooted there).
- `cmd/init_redesign.go` — replaces `newVesselStageStub` / `newSoilStageStub` calls with `NewVesselStage(ctx)` / `NewSoilStage(ctx, options)`. Added `scaffoldingToSoilOptions(cat)` helper mirroring the legacy `scaffoldingOptions` mapper but producing `initflow.ScaffoldingOption` shape. Branches + Observe stay as `NewStubStage`; legacy generate/conflict tail still skipped.

## Pre-dispatch refinements (decisions resolved before agent ran)

1. **Vessel `Result()` shape** → `map[string]string` (keys `name`, `description`, `station`). Single stage replaces three legacy steps, so `prev[]` becomes per-stage, not per-field — `runInitRedesign` will read `vessel := prev[0].(map[string]string); name := vessel["name"]`.
2. **Soil implementation** → hand-rolled, NOT `bubbles/list`. 4–8 items, `bubbles/list` ships fuzzy filter + pagination + own row styling (hard to match `zen-shell.jsx` ZStepScaffolding layout). Hand-roll is ~120 LOC with full control over focus highlight, required-badge, glyphs.
3. **Header station-strip bundled in Phase 3** — user surfaced the bug at session start ("top right we have 'Planting into', there we have station, but thats wrong, as station is not created yet"). Fix is <15 LOC in same package (`initflow`) with one caller (`Stage.renderFrame`) inside the package — low-risk bundle vs. its own tiny PR. Added to Phase 3's Files touched + Steps + Verification in the plan.

## Authorized divergence noted in review

**Validator inlining in Vessel.** `stationDirValidator` + `normaliseDocsPath` live in `cmd/`. `initflow` cannot import `cmd` (cycle). Agent inlined the logic (`validate()` + station-normalization in `Result()`) with one plan-authorized behavior difference: empty Station falls through to the `station/` default rather than rejecting as empty. Everything else — reject `"/"`, trim + trailing-slash append, error message text — matches `cmd/init.go`'s originals bit-for-bit. Reviewer confirmed fidelity.

## CI

All 6 checks green on PR #49: test, Analyze Go (CodeQL), lint (golangci v2.11.4), govulncheck, CodeQL, GitGuardian. Post-merge on main: `make build` clean, `go test ./...` all green.

## Merge + cleanup

- `gh pr merge 49 --squash --delete-branch` — squashed to `971ee44`.
- Known gotcha recurred: local branch delete failed ("checked out at worktree"). Followed the pattern in memory (`git push origin --delete <br>` + `git worktree remove -f -f <path>` + `git branch -D <br>`). Clean.

## Next up

**Phase 4 — Branches tabbed picker + inline-expand.** Replaces the Branches stub (currently stage 2) with a tabbed category picker driving per-category item lists. Inline-expand on focused row shows item description. Scope sketched in plan §"Phase 4 — Branches tabbed picker + inline-expand" starting at line 478.
