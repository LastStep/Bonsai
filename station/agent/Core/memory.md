---
tags: [core, memory]
description: Tech Lead Agent working memory — flags, work state, notes.
---

# Working Memory

## Flags

<!-- Active flags go here. Format: - [FLAG] description -->

(none)

## Work State

**Current task:** Idle. **Plan 29 complete** 2026-04-23 — init + add bug bundle (PR #72 squash `9eb2bff`). 8 phases across 8 bugs + 4 test gaps surfaced by deep-dive audit of `cmd/init_flow.go` + `cmd/add.go` + `internal/tui/addflow/` + `internal/tui/initflow/`. Fixes: (A) `cmd/add.go:77-85` `HeaderAction: "INIT" → "ADD"` + `HeaderRightLabel: "PLANTING INTO" → "GRAFTING INTO"` — stale inheritance from init_flow.go since Plan 28 Phase 1; add-flow header was reading "INIT · PLANTING INTO" for the add command. (B) conflicts.go dead `w := s.Width()` dropped from `View` + `listHeight()` `fixedRows` 15→14 reconciled w/ actual non-list row count (list gets +1 row on 20-row terminals). (C) grow.go redundant `SetRailHidden(true)` dropped — `bodyOnly` branch in `GenerateStage.View` bypasses `renderFrame` entirely. (D) yield.go stale "rail position 5" doc → "rail position 3 (StageIdxYield)" + duplicate `totalAbilities` computation collapsed. (E) ground.go dead `_ lipgloss.Position` reserved field dropped (gofmt realigned adjacent comments — documented deviation). (F) 4 test gaps filled: `TestConflicts_ViewportFollowsFocus` / `TestConflicts_ColorTonesDifferPerAction` / `TestConflicts_LowercaseKMovesFocus` / `TestGenerateStage_BodyOnlyDropsChrome`. (H) **Path-traversal validator hardening** — new `invalidWorkspaceReason(ws string) string` helper in `ground.go` rejects absolute paths (`filepath.IsAbs`) + `..` segments (post-`filepath.Clean`), wired into `Update`'s Enter branch AFTER `NormaliseWorkspace` BEFORE duplicate-check. Mirrored check added to `vessel.go:validate()` for STATION input w/ `path/filepath` import. 5 new validator tests: `TestGround_RejectsAbsolutePath`, `TestGround_RejectsParentEscape`, `TestGround_RejectsHiddenParentEscape` (`nested/../..` case exercising post-Clean reduction), `TestVessel_RejectsAbsoluteStation`, `TestVessel_RejectsParentEscapeStation`. Plan 27's security callout ("C6 inherits Ground-stage validation (non-empty, no `..`, no absolute paths, collision check)") was aspirational then — this plan lands it. `TestNormaliseWorkspace` fixtures unchanged + still passing (no over-rejection). Net +259/−34 across 10 files (7 phase-scoped commits). Process: deep-dive audit → plan spec w/ phase-level `file:line` targets committed at `87062c4` → single dispatch off that tip (`isolation: worktree`) → PR returned clean w/ 2 documented deviations (gofmt realignment + test-file-local `shortName` helper) → parallel code-review + security-review agents → both **PASS zero blockers** (3 code nits + 2 code test-gaps + 4 security hardening minors all filed to Backlog as Plan-29-cosmetic + Plan-29-test-gap + Plan-29-security-hardening) → 6/6 CI green → squash-merge. Worktree-held-branch pattern 12× this month. **Coordination:** Parallel session landed Plan 28 Phase 2 (list, PR #71 `9ce25a4`) + Phase 3 (guide, PR #70 `ee55237`) start-to-finish concurrently w/ Plan 29. Scope-lock in plan spec ("Do NOT touch `cmd/guide.go` / `cmd/catalog.go` / `cmd/list.go` / `internal/tui/{listflow,guideflow,catalogflow}/`") kept dispatched agent disciplined — zero collision despite 4 merges landing on main in rapid sequence. Parallel session's Plan 28 close-out commit (`c8d66f8`) absorbed my uncommitted Plan 29 archive + Backlog edits via shared index — benign; Status + memory reconciled in follow-up. **Prior:** 2026-04-23 Plan 28 Phase 2 + Phase 3 — `bonsai list` cinematic (PR #71 squash `9ce25a4`, +1163/−116 across 7 files) + `bonsai guide` cinematic (PR #70 squash `ee55237`, +1092/−43 across 7 files). Parallel dispatch off `f8f90de` (decision deltas commit — added D1 drop-kanji, D2 50-cap file-tree, D3 `bonsai update` workspace-missing CTA, D4 no-arg+non-TTY exit-1 error, D5 no Phase-1-NIT bundle, D6 parallel strategy). Phase 2 implementation +1125/−150 → review PASS-WITH-MINORS (4 correctness + 2 stale comments + 1 test side-effect + 5 test gaps) → fix-agent `80baab5` (+130/−58): dropped buggy `strings.Contains("..")` false-positive (rejected legitimate `my..workspace` names — rely on `filepath.Rel` ancestor check alone, canonical defense), dropped unused `termW` param from `RenderAgentPanel`, deleted `statDir` pass-through wrapper w/ false docstring, stripped dead `d.IsDir()` branches inside symlink handler (`DirEntry.IsDir()` always false for symlinks — `filepath.WalkDir` default non-following is the actual defense), 2 stale comment strips, moved `TestMain` catalog wiring out of package scope into `setupListTestCatalog(t)` helper w/ `t.Cleanup` restore (prevents broad side-effect on every `cmd/` test), new tests `TestRenderAgentPanel_AbsolutePathEscape`/`SkipsNodeModulesAndGit`/`LegitimateDotDotInName`. Phase 3 implementation +1042/−31 — initial CI lint FAILURE on `cmd/guide.go:19,28` dead `guideTopic` struct + `guideTopics` var leftover from pre-rewire (agent's self-test missed `golangci-lint unused`; local `go test` doesn't run linter — recurring gotcha documented 2026-04-17 as "Local `go build` + `go test` miss `golangci-lint unused`") — fixed via direct worktree edit commit `39fc711` (19-line strip) → review PASS-WITH-MINORS (4 cosmetic/correctness + 1 test gap) → fix-agent `4527653` (+89/−32): dropped unused `width` param from `buildTabStrip`, fixed narrow-width short-label trigger (original `narrowStripThreshold=60` + `budget<60 → budget=60` floor made short-label path unreachable — now measures full-strip width against `initflow.PanelWidth(width)` only), `errors.New(noTTYNoArgErr)` over `fmt.Errorf("%s", noTTYNoArgErr)`, dropped redundant `TerminalTooSmall` check in `View()` (fix-agent verified `Stage.RenderFrame` at `stage.go:251` already short-circuits on same predicate before trusting), new `TestViewer_ScrollKeyDelegation` asserting `j`/`k`/`f`/`b` → `viewport.YOffset` delta via `DefaultKeyMap` alias path. Both PRs 6/6 CI green (test/lint/Analyze-Go/govulncheck/CodeQL/GitGuardian). Merged PR #70 first `ee55237`; parallel session landed `87062c4` (plan-29 spec, disjoint planning-doc file) mid-sequence; PR #71 rebased trivially clean (no file overlap w/ #70 or `87062c4`), merged `9ce25a4`. Worktree-held-branch cleanup hit 10× + 11× this month — manual `git worktree remove -f -f .claude/worktrees/agent-*` + `git branch -D plan-28/phase-*-cinematic` + `git push origin --delete plan-28/phase-*-cinematic` both cycles. Cross-phase verification: `make build && go test ./...` clean (dev binary), `bonsai --help` shows 8 commands (no `completion` per D13 Phase 1), `bonsai completion zsh | head` still emits `#compdef bonsai` (command functional, hidden from help), `./bonsai guide </dev/null` exits 1 + exact D4 error, `./bonsai list </dev/null | head` renders cinematic chrome/scaffolding/panel/file-tree/footer, `./bonsai catalog </dev/null | head` renders static 7-section fallback. Plan archived at `Playbook/Plans/Archive/28-view-cmds-cinematic.md`. Prior: 2026-04-22 Plan 26 — P2 knock-off bundle (PR #66 squash `a9df552`). Net +149/−14 across 5 files. Ships: (A) `station/agent/Sensors/context-guard.sh` lines 156-157 — `os.path.join(root, "")` → `docs_path` (already defined line 24 as `root/station/`); planning-reminder now injects paths that actually resolve (harmless-but-misleading pre-existing bug); matches wrap-up checklist path style at lines 122-124. (B) `station/agent/Core/routines.md` — dropped 2 blank lines (orig 38 + 55) splitting Dashboard + Definitions markdown tables; diagnosis in original Backlog entry was wrong (claimed `generate.go:884` frequency grouping) — actual root cause was stale file output of older generator version, current `RoutineDashboard()` at `internal/generate/generate.go:917-1061` already emits contiguous body rows. Lockfile hash synced in main worktree only (`.bonsai-lock.yaml` gitignored, not in PR). (C) `cmd/root.go:173` + `cmd/add.go:329` — replaced `filtered := slice[:0]` aliasing w/ `make([]string, 0, len(slice)-len(dropped))` pre-allocation. Capacity arithmetic safety verified at both sites: in root `dropped ⊆ selected` (populated by iterating selected); in add `dropped ⊆ toBackup ⊆ toOverwrite` (toBackup pushed into toOverwrite at line 304). (D) `cmd/add.go` renamed inner closures `installedSet` → `installedItems` at lines 532 + 619 + 6 internal call sites (541/550/559/628/639/650); file-scope `func installedSet(cfg *config.ProjectConfig) map[string]bool` at line 349 + external caller at line 119 untouched. (E) New `TestRoutineDashboardNoBlankRows` in `internal/generate/generate_test.go` (+137L) — builds MapFS catalog w/ shared core + 7 tech-lead-default routines at mixed 5/7/14d frequencies, calls `RoutineDashboard` into temp dir, scans result for (i) no blank row between dashboard body rows inside `ROUTINE_DASHBOARD_START`/`END` markers — tolerates generator's expected blanks flanking markers via `seenBodyRow` gate, (ii) no blank row in Routine Definitions body, (iii) every routine display name present. Process: pre-flight found 4 uncommitted station bookkeeping files from prior session's v0.2.0 wrap-up — committed as `1b49963` before dispatch (station-only, direct-to-main per fast-iter convention), push triggered push-CI which passed. Wrote plan `Playbook/Plans/Active/26-p2-knockoff-bundle.md` (Tier 1, single dispatch); committed + pushed as `fb6ac8c` pre-dispatch so agent worktree saw spec. Dispatched general-purpose agent w/ `isolation: worktree`; agent self-recovered from Edit-tool-writes-to-absolute-path mistake (initial edits landed in main worktree, agent re-applied via `git apply` to worktree + `git checkout --` reverted main before reporting — pre-merge main verified clean). Agent created draft PR #66 clean-scope; independent code-review agent returned **PASS zero findings** (no major/minor/nit); 6/6 CI green (test/lint/Analyze-Go/govulncheck/CodeQL/GitGuardian); squash-merged same session. Post-merge: worktree held branch (pattern ~7× this month) → manual `git worktree remove -f -f .claude/worktrees/agent-a0807188` + `git branch -D plan-26/p2-knockoff-bundle worktree-agent-a0807188` + `git push origin --delete plan-26/p2-knockoff-bundle`. Plan archived. Prior: 2026-04-22 **v0.2.0 release ship** — pre-release docs audit bundle (PR #65 squash `e9b7cba`) covering P0+P1+P2 findings from 4-surface audit (website, root community files, catalog, station/). Bundle: (A) sensor count 12→13 everywhere via `website/scripts/generate-catalog.mjs` (auto-synced `catalog.json`+`overview.mdx`+`sensors.mdx`) + manual README line 158; (B) tagline mechanical replace "structured language" → "workspace" across `index.mdx` (description+tagline+body), `astro.config.mjs`, `why-bonsai.mdx`; (C) invalid agent types stripped — `review-checklist/meta.yaml` drops `reviewer`, `test-strategy/meta.yaml` drops `qa` (neither agent type exists); (D) LICENSE 2025 → 2025-2026; (E) Go 1.24+ → 1.25+ in README + CONTRIBUTING; (F) `station/CLAUDE.md.bak` deleted (gitignored Plan 01 leftover); (G) `Plans/Archive/23-uiux-phase2-add.md` frontmatter `Status: Draft` → `Complete`; (H) `station/code-index.md` refreshed — drops deleted `cmd/add_redesign.go`, repoints `init_redesign.go` → `init_flow.go`, line-drift fixes across catalog/generate/config/styles/prompts/root, adds tables for `internal/tui/harness/` + `initflow/` + `addflow/` (12 stages + 10 entry points were missing); (I) `docs/README.md` rewritten to clarify embedded-binary (`bonsai guide`) vs Starlight-website split; (J) `CHANGELOG.md` `[Unreleased]` cut to `[0.2.0] - 2026-04-22` with curated entries grouped Added/Changed/Removed/Fixed/Security covering Plans 19-25 + statusLine + push-CI widening + binary path fix + Go bump; v0.1.0–v0.1.3 dates backfilled `2026-04-15` → `2026-04-16` to match git tag creator dates (from `git for-each-ref --sort=creatordate` — all four tags created same day); link refs rewired (`[Unreleased]` → `v0.2.0...HEAD`, new `[0.2.0]` → `v0.1.3...v0.2.0`). 15 files, +224/−96. Process: Q-verification pass before any edit caught 2 false-positive findings (workspace-guide `.md.tmpl` works as designed per `catalog.go:361,456` both-suffix support + `generate.go:782` special-case; skills-frontmatter missing on 13 of 17 is cosmetic since loader reads triggers from meta.yaml not file frontmatter — deferred to Plan 26). Self-executed P0+P1 as mechanical edits (commit `1be622a`), pushed branch, dispatched general-purpose agent for P2 + CHANGELOG synthesis (3 commits `ff24fce` + `c7d9442` + `c05fca4`); PR #65 opened, 8/8 CI green (test/build/lint/govulncheck/CodeQL/Analyze-Go/GitGuardian, deploy-skip push-only); squash-merged. Parallel session pushed 2 unrelated commits mid-work to main (`d5a7dc9` + `d25eef1`, non-overlapping files, clean merge forward). Tag `v0.2.0` annotated + pushed → Release workflow triggered → GoReleaser built + uploaded 6 platform binaries (darwin/linux/windows × amd64/arm64) + checksums to GitHub Release v0.2.0 (**Release published**) → **Homebrew step failed 401 Bad credentials** (`HOMEBREW_TAP_TOKEN` PAT expired per `.github/workflows/release.yml:36`; error at `goreleaser` step "could not check rate limits" + "error checking for default branch"). **Recovery path chosen:** manual formula push (not workflow retry, since no `workflow_dispatch:` trigger on release.yml AND re-running with `--clean` on existing tag risks GoReleaser recreating release with new timestamps/caching-break). User rotated PAT + ran `gh secret set HOMEBREW_TAP_TOKEN --repo LastStep/Bonsai`; tech-lead pushed v0.2.0 formula to `LastStep/homebrew-tap/Formula/bonsai.rb` directly via `gh api -X PUT /repos/LastStep/homebrew-tap/contents/Formula/bonsai.rb` with 4 platform SHAs from `checksums.txt` (commit `29fd0da` on tap). Live verification: `gh api` on tap contents/bonsai.rb shows v0.2.0 + all 4 SHAs correct (raw.githubusercontent CDN has stale cache, normal eventual consistency). All pre-OSS-launch gates cleared. Prior: 2026-04-22 Plan 23 **complete** — Phase 3 cutover + 7 bundled backlog cleanups (PR #64 squash `788fa6c` + post-review fix `44e6874`). Net +947/−990 across 9 files. Cutover ships: (A) `BONSAI_ADD_REDESIGN` env gate deleted; cinematic `runAdd` body lives in `cmd/add.go`; (B) legacy delete — `runAdd` legacy body, `runAddSpinner`, `buildNewAgentSteps`, `buildAddItemsSteps`, `addOutcome` struct, `normaliseWorkspace`, `workspaceUniqueValidator`, `newDescriber`, `userSensorOptions` (latter from `cmd/init.go`), Phase-1-deferred `NewYieldAddItemsDeferred` + `yieldModeAddItemsDeferred` + `renderAddItemsDeferred()` all gone (zero grep hits); (C) `cmd/add_redesign.go` deleted, `cmd/init_redesign.go` → `cmd/init_flow.go` via `git mv` (96% rename). Bundled cleanups: #1 dead post-harness Generate-error warning deleted from BOTH `cmd/init_flow.go` AND `cmd/add.go` (latter symmetric path caught by code review minor on top of plan, fixed in `44e6874`); #2 three harness composition tests for `NewConditional(NewLazy(...))` (`internal/tui/harness/steps_test.go` — Chromeless forwarding + skipped path + builder-fires-once-per-pass with Reset re-fire); #3 `growSucceeded` predicate uses closure capture `outcome.SpinnerErr == nil && outcome.Ran` instead of walking `prev[]` for any error (predicate doc explicitly justifies safety: ConditionalStep evaluates at Init time on Yield slot, after harness drains Grow's generateDoneMsg, so outcome fields are stable); #4 new `addflow.NewYieldUnknownAgent` variant with "Unknown agent type — catalog may be stale, run `bonsai update`" CTA replaces unknown-agent path's misleading reuse of `YieldTechLeadRequired` + test asserts `bonsai update` in copy; #5 `.bak` write-error silent-discard fixed in BOTH `applyCinematicConflictPicks` (cmd/add.go) AND `applyConflictPicks` (cmd/root.go) — on read OR write failure for `.bak`, that path is dropped from overwrite list, single collected `tui.Warning` lists all dropped paths, helpers early-return false if every path dropped (security review confirmed PR #62 regression closed in both flows); #6 conflicts post-harness slot resolved by `for _, r := range results { if picks, ok := r.(map[string]config.ConflictAction); ok { ... break } }` instead of `len(results)-2` arithmetic; #7 new `cmd/add_test.go` (368L) — 9 table tests covering Keep/Overwrite/Backup/mixed/empty/backup-read-fail/backup-write-fail/all-dropped/dropped-list-contains-all using `t.TempDir` + `chmod 0500` + stdout pipe capture (windows + root skip), no mocks. Process: pre-dispatch revised plan + pushed `78122a3` (direct-to-main per fast-iter UX convention) so agent worktree saw Phase 3 spec; dispatched general-purpose agent off `78122a3` worktree; agent returned draft PR #64 clean; ran independent code-review + security-review agents in parallel — security PASS no findings, code PASS + 4 minors (2 real: dead post-harness warning in add.go + test rename, 2 cosmetic: `selected[:0]` aliasing + `installedSet` shadowing); dispatched 2nd worktree fix-agent on same branch for the 2 real minors → commit `44e6874` (5 lines net); 6/6 CI green; squash-merged. 2 cosmetic minors filed to Backlog as `[Plan-23 cosmetic]`. Post-merge: `gh pr merge --delete-branch` left remote branch behind (worktree-checked-out gotcha hit again — pattern is now ~6× this month) → manual `git push origin --delete` + 2× `git worktree remove -f -f` + `git branch -D`. Also: main worktree got switched to detached HEAD at `7a1ae2d` mid-session (suspect fix-agent's `git checkout -B worktree-agent-ad57b8f4` somehow leaked into main worktree despite `--isolation worktree`) — fixed via `git checkout main` before merge. Plan archived. Prior: 2026-04-22 Plan 23 Phase 2 — `bonsai add` cinematic add-items branch + `ConflictsStage` (PR #62 squash `61777c0`). Net +1099/−57 across 9 files. Ships: (A) splicer wires real add-items sub-sequence `[AddItemsGraft, Observe]` replacing Phase 1's `YieldAddItemsDeferred` terminal splice — Ground skipped, `AgentDisplay` + new `SetDefaultWorkspace` stamped from `installedAgent.Workspace`; (B) `buildAddGrowAction` type-scans `GraftResult` from prev and handles both branches — add-items arm mirrors legacy `runAddSpinner` via shared `distributeAddItemPicks` so category slices are bit-identical; (C) `internal/tui/addflow/conflicts.go` (438L new) chromeless tabbed stage — one tab per conflict file, 3-way radio (Keep/Overwrite/Backup, default Keep), `Result()` returns defensive-copy `map[string]config.ConflictAction`, keystrokes `← → / h l` tab wrap + `↑ ↓ / j k` radio no-wrap + `␣ / ↵` advance + `shift+tab / esc` back; (D) rail extended 6→7: `衝 しょう CONFLICT` at idx 5, Yield → idx 6; (E) `initflow.ConflictRowStyle(tone)` real body + `ConflictActionGlyph(tone)` (Success/Danger/Warning palette tokens per action — Phase 1 stub replaced); (F) `internal/config/lockfile.go` new `ConflictAction` enum (Keep/Overwrite/Backup) — in-memory transport only, no yaml tag, never persisted, zero-value = Keep (safe default); (G) `applyCinematicConflictPicks` post-harness helper drives `.bak` writes + `wr.ForceSelected` — legacy `applyConflictPicks` untouched (still used by remove/update/legacy add); (H) Observe `SetPrior` refactored from positional to type-scan (handles both prev shapes: new-agent `[string,string,GraftResult,bool]` vs add-items `[string,GraftResult,bool]` without branch-specific ctor) + `SetDefaultWorkspace` so splicer can seed workspace where Ground is skipped; (I) 12 new tests. Pre-dispatch process: took over in-tree WIP from prior session, reverted bad `SetRailLabels(initflow.StageLabels[:])` flip in 4 callsites (would have rendered init's 4-label array in add's 6-stage rail — caught via label-count mismatch), committed good hunks in 2 direct-to-main commits: `6b5ef7f` station bookkeeping + `3308a7d` predicate type-scan refactor (addresses Backlog C1) + SetRailHidden + uniform 4-cell anchor + chromeless Yield + observePanelW cap + CTA copy tighten. Then dispatched general-purpose agent via worktree isolation off `3308a7d`; agent returned draft PR #62 with clean scope. Reviews: independent code review PASS (3 MINOR + 4 NIT deferred to Backlog — test gap: `applyCinematicConflictPicks` no direct unit test), security review MINOR_FINDINGS (3 items all deferred: `.bak` write-error silent-discard carry-forward from legacy `applyConflictPicks`, TOCTOU on Backup read, defensive picks-key intersect hardening). 6/6 CI green; squash-merged. Post-merge cleanup: worktree held branch → local delete failed via `--delete-branch` → manual `git worktree remove -f -f` + `git branch -D` + `git push origin --delete` (documented pattern hits again). Prior: 2026-04-22 Plan 25 — README revamp (PR #61 squash `d6785e7`). Ships: (A) logo-slot HTML comment at top of `README.md` (no visible empty `<div>`); (B) tagline swap "A structured language for working with AI agents." → **"A workspace for your coding agent."**; (C) `status-early-stage` shield badge added; (D) new `## Who Bonsai is for` section — audience hook for solo devs + small teams, 2-sentence problem framing, 5 mechanism-over-personality bullets anchored to concrete files (`SessionStart` hook / protocol files / `Playbook/Plans/Active/NN-*.md` / `agent/Skills/review-checklist.md` / `Logs/KeyDecisionLog.md` + `Playbook/Backlog.md` + `Reports/`), "Not just CLAUDE.md with extra steps" rebuttal, `station/` tree snippet showing default `bonsai init` layout; (E) merge old `## Why Bonsai` + `## What's Inside` into `## How it works` — layer-stack diagram preserved, graph-view PNG demoted from hero to supporting visual, Six agent types table + catalog counts + Extensible folded in; (F) delete `## Documentation` + `## Contributing` as standalone sections → fold into footer nav; (G) cut AI-readme smells (reviewer-flagged): `by design`, `powerful out of the box`, `One binary. No runtime. Works with any project.`, `teammates, not tools`. Process: spawned independent general-purpose research agent pre-draft for critical review of proposed structure + tagline options + mechanism bullets. All high-impact findings adopted. 7/7 CI green. Prior: 2026-04-22 Plan 23 Phase 1 — addflow cinematic new-agent path (PR #59 squash `163bc45`). Ships: (A) new `internal/tui/addflow/` package mirroring initflow shape — 6-stage cinematic flow (選 Select · 地 Ground · 接 Graft · 観 Observe · 育 Grow · 結 Yield), chromeless stages composing initflow chrome (header/enso rail/footer/MinSizeFloor), per-stage Kanji+kana+English labels with ASCII fallback, `GraftResult` pick payload, `Outcome` scratchpad; (B) initflow additive exports — `CenterBlock`/`PadRight` aliases, `Stage.SetRailLabels`/`SetRailIndex`/`SetLabel`/`SetSize`/`RenderFrame` + read-accessors, `GenerateStage.SetBodyTitle` kanji override, `RenderEnsoRail(labels)` signature accepts custom label slice (nil → init default); (C) `cmd/add_redesign.go` — 311-line harness wiring `runAddRedesign` behind `BONSAI_ADD_REDESIGN=1` env gate; `cmd/add.go` 7-line env-flag branch at top of `runAdd`; (D) 46 unit tests across select/ground/graft/observe/yield. Process: initial PR from parallel session had lint FAILURE (2 gofmt issues) + cross-track regressions (branched from `7c1dd49` before `f24750d`/`674f987` landed on main, so PR diff reverted 4 station/ files + re-introduced the "Plant ~N files into" misleading CTA). User authorized me to take over on the branch; rebased on origin/main (clean, dropped 6 files), fixed B2 gofmt via `gofmt -s -w graft.go observe.go` (commit `babb3df`), fixed B1 silent-misroute bug (commit `885d128`): splicer was routing BOTH "all-installed" AND "add-items has available" to `YieldAllInstalled` — the partial-installed case lied to the user with "already full". Added `yieldModeAddItemsDeferred` + `NewYieldAddItemsDeferred` + `renderAddItemsDeferred` body surfacing "Phase 2 coming — unset BONSAI_ADD_REDESIGN + re-run via legacy" CTA; splicer now routes partial-installed there. Force-pushed `--force-with-lease`, 6/6 CI green, squash-merged + local/remote branch cleanup. C3 (Yield.Result() missing) turned out to already exist at yield.go:318 — misread on first scan. Prior: 2026-04-22 Plan 24 — pre-launch polish bundle (PR #58 squash `4ef8271`). Ships: (A) `CHANGELOG.md` keep-a-changelog 1.1.0 curated v0.1.0–v0.1.3 backfill + `[Unreleased]` stub + link refs; (B) `.github/workflows/docs.yml` adds `pull_request` trigger + `if: github.event_name == 'push'` guards on deploy job + upload-pages-artifact step (Option 1 — same "Deploy Docs" failure surface for PR and main); (C) root `Bonsai/CLAUDE.md` `internal/tui/` tree refresh (styles_test.go, filetree*, harness/, initflow/ — subdirs only, no per-file enumeration); (D) Backlog Group C/D consolidation (2 entries removed + removal comments, Group D suffixed as "refiled as good-first-issue"); (E) 5 GitHub issues filed (#53 relabeled + #54/#55/#56/#57 new) all `good first issue` + `help wanted`. CI gotcha: pre-existing gofmt drift in `internal/tui/initflow/observe.go:466` + `planted.go:423` (from earlier direct-to-main polish) caught on PR lint — fixed on PR branch via `gofmt -s -w` before merge. Prior: 2026-04-22 post-Plan-22 12-point dogfood polish + statusLine redesign (commits `018966d` `4a8fea9` `975e15d`, all direct-to-main). Prior: 2026-04-21 Plan 22 Phase 5B (PR #52 squash `5916e05`) — wired `NewConditional(NewLazy(GenerateStage))` + LazyGroup conflicts + `NewConditional(NewLazy(PlantedStage))` into `runInit` at `cmd/init_redesign.go`; renamed `runInitRedesign` → `runInit`; deleted legacy 245-line `runInit` body + `buildReviewPanel` + `scaffoldingOptions` + `BONSAI_REDESIGN` env branch from `cmd/init.go`. Harness additions: `LazyStep.Chromeless()` + `ConditionalStep.Chromeless()` delegation, `ConditionalStep.Init` auto-builds nested LazyStep — necessary because `GenerateAction = func() error` needs prev-capture at build time (plan's literal inline ctor wouldn't fire). Split single `plantedConfirmed` predicate from plan into `observeConfirmed(prev[3] bool)` + `generateSucceeded(observeConfirmed && prev[4] non-error)` so Generate-failure skips Planted. `buildGenerateAction` closes over 10 params; `plantedSummary(installed)` lazy so post-`EnsureRoutineCheckSensor` counts render. +260/−297 across 8 files. 6/6 CI green. Independent review PASS + 2 minors (composition test + dead-path warning — filed to Backlog). Prior 2026-04-21: Plan 22 Phase 5A PR #51 squash `6baaf8e` + lint fix `3e31967` — responsive resize foundation (`MinTerminalWidth=70`, `ClampColumns(120)=(24,44,12)` regression anchor, hand-rolled Viewport, `RenderMinSizeFloor`), Branches/Vessel/Soil retrofit, ObserveStage wired, GenerateStage + PlantedStage packaged. Prior: 2026-04-21 Phase 4 dogfood polish run (direct-to-main, no PRs per fast-iter UX convention): `413e360` header left block split (`[ 盆 ]` row + INITIALIZE row) + per-tab 2-line intro + DETAILS box moved below list (fixed-height, no jitter) + right-aligned DEFAULT tag; `399fe08` density cut (row 60 cells) + word-wrapped details (2 rows) + centered tab counts via `lipgloss.PlaceHorizontal(colW, Center, ...)` ; `eaee416` widened everything back (nameColW 24, descColW 44, tab colW 16, row 84 cells) + rune-aware name truncation so DEFAULT/(required) always stays in its rightmost column; `6bb74e5` ABOUT/FILE values → `ColorAccent` (white) + 3-row wrap × 70 cells (210 absorbs every catalog description; dispatch-guard ~111 chars previously clipped); `fa0ae64` kanji centered via `[ 盆 ]` padding + extra blank line between DETAILS and counter. Helper `wrapToWidth` (word-break, rune-fallback hard-wrap) added to `branches.go`. Prior: PR #50 `89c21ba` — Plan 22 Phase 4: `BranchesStage` (branches.go 582L, branches_test.go 380L) — 5-tab picker across Skills/Workflows/Protocols/Sensors/Routines with per-category `branchCat` + `branchItem`; `← → / h l` tab cycle wraps; `↑ ↓ / j k` focus clamp no-wrap; `␣` toggle (no-op on required); `?` global `expanded` flag toggles inline-expand on focused row; `↵` advance; `esc/shift+tab` propagate. `BranchesResult{Skills,Workflows,Protocols,Sensors,Routines []string}` shape; catalog-sorted machine names; required items always in Result. Constructor `NewBranchesStage(ctx, cat, agentDef)` — builds `[]branchCat` internally via `cat.SkillsFor`/`WorkflowsFor`/`ProtocolsFor`/`SensorsFor`/`RoutinesFor("tech-lead")`; required via `item.Required.CompatibleWith("tech-lead")`; defaults from `agentDef.DefaultSkills`/etc. Inline-expand renders ABOUT + FILE only (catalog lacks `Affects`/`CrossLinks` today — fields reserved on struct for future expansion per plan note). Kanji 技/流/律/感/習 with ASCII fallback. Focus: leaf `│ ` border-left, no forced bg (Soil precedent). `Reset()` clears `done` only — preserves `categories`/`catIdx`/`itemIdx`/`selected`/`expanded` (plan §verification explicit). Wire: `cmd/init_redesign.go:78` stub→real; Observe at idx=3 still stub. 12 tests: tab cycle both dirs with wrap, focus clamp, toggle non-required, required no-op, `?` expand, Result per-tab, Result catalog order, Enter completes, Reset preservation, cross-tab selection persistence, defaults applied, required always in Result. Independent review PASS on all 10 locked decisions + 7 keybindings. Net 963 adds / 1 del across 3 files. CI green (test/Analyze Go/lint/govulncheck/CodeQL/GitGuardian). Process: pushed local-only `380dbc5` (Phase 3.5) to origin/main pre-dispatch so agent worktree saw current palette — per memory's "Agent worktrees base off origin/main" gotcha. Prior: PR #49 `971ee44` — Plan 22 Phase 3: `VesselStage` + `SoilStage` + header strip. Prior: PR #48 `2e2a08c` — Plan 22 Phase 2: initflow package + `harness.Chromeless` + env-flag routing. Prior: PR #47 `7553d43` — Plan 22 Phase 1: `RenderFileTree` widget + palette tokens.

## Notes

<!-- Session-to-session notes. Keep concise. -->

- **Plan 14 code shipped to main via PR #24 bundle (2026-04-17).** Iterations 1 + 2 (palette tokens, banner, answered-prompt persistence, required-only feedback, category counts, prompt polish, mustCwd error surfacing, width-aware TitledPanel) merged along with Plan 17's release-prep work. Phase 4+ scope (screen lifecycle, progressive disclosure, go-back nav, flow redesign) still deferred — Plan 15's BubbleTea harness is the likely vehicle for those.
- **Parallel session convention.** `ui-ux-testing` branch is where Plan 15 iterations land (separate session). Don't cross-pollinate planning-doc edits between branches — each branch's Status.md/memory.md should stay scoped to its own track. Merging to main collapses everything cleanly.
- **CI lint gotcha.** `golangci/golangci-lint-action@v6` with `version: latest` resolves to v1.x, not v2. `.golangci.yml` must use v1 schema (`linters.disable-all: true`, no `version:`/`formatters:` keys). If we ever move to v2, update both the action pin (`version: v2.x`) and the config schema together. Learned from PR #24 `66a6304`.
- **Plan 08 Phase C (new sensors) paused** — moved back to Pending while Plan 14 ships. Resume once UI/UX overhaul series wraps or explicitly requested.
- **Pre-flight learning:** Worktrees inherit only committed HEAD — uncommitted plans/docs in main tree are invisible to dispatched agents. Commit station/ planning artifacts before dispatch.
- **PR review memory hygiene:** "both reviews APPROVE" from prior session was dispatched review agents, not GitHub reviews. `gh pr view --json reviews` returned empty. When noting review status, distinguish agent-dispatched reviews (in `Reports/`) from GitHub formal reviews.
- **Squash-bundle rebase pattern.** When main has absorbed a feature branch's commits via squash-merge (e.g., Plan 14 individual commits → bundled into Plan 17's `bc565bf` squash), straight `git rebase main feature-branch` will conflict on every doc snapshot because main has moved past those states even though the *code* matches. Cleaner approach: `git reset --hard main && git cherry-pick <only-the-truly-new-commits>` after listing each commit and asking "is this code/content already on main via the squash?" Always preserve old tip as `<branch>-pre-rebase` first. Used 2026-04-20 to rebase `ui-ux-testing` (13 commits → 3 commits actually carried forward).
- **Worktree cwd gotcha from Claude Code.** `git worktree add ../<name> <branch>` runs in the bash tool's cwd, which is often a subdirectory (for us: `station/`), not the repo root. So `../Bonsai-uiux` resolves to `/home/rohan/ZenGarden/Bonsai/Bonsai-uiux` (nested inside the main worktree — violates the no-nesting rule), not the intended sibling `/home/rohan/ZenGarden/Bonsai-uiux`. **Always use absolute paths when creating worktrees from this session.** Hit this 2026-04-20 setting up the Plan 15 parallel-session worktree; caught, removed, recreated at the correct sibling path.
- **Subagent tool inheritance is flaky.** On 2026-04-17, the original executing agent (worktree, `gh` authed) successfully created draft PR #23, but every subsequent subagent I spawned for merge/verification reported `gh: command not found` — the environment wasn't inherited from the spawning agent or from my own shell (Windows Git Bash over WSL UNC path, no `gh` on PATH). Implication: for PR-flow tasks, bundle **all** gh operations (push, create PR, mark ready, merge, delete branch) into a **single** agent dispatch rather than splitting across agents. Second-best: ask the user to click-merge via web UI when a subagent-less step is needed.
- **PR CI ≠ main CI.** The `CI` workflow (test + lint + GitGuardian) runs on pull_request, but `Deploy Docs` only runs on `push` to main. So a broken `website/**` change can pass PR CI and then fail on main — happened 2026-04-20 with PR #25's MDX autolink. When reviewing PRs that touch `website/`, run `cd website && npm run build` locally before merging, or add a `website/**` path trigger to Deploy Docs on pull_request (separate backlog item).
- **MDX autolink gotcha.** `<https://example.com>` is valid GitHub-flavored markdown but MDX parses `<` as JSX — Astro build fails with "Unexpected character `/`". Always use `[label](url)` inside `.mdx` files. Incident: PR #25 `guide.mdx:20`, fixed in `e336ccb`.
- **Local `go build` + `go test` miss `golangci-lint unused`.** Plan 15 iter 3 left `resolveConflicts` in `cmd/root.go` as dead code after the harness migration. Local verification (`make build`, `go test ./...`, `go vet ./...`) all passed; only the CI `lint` step (golangci-lint v1.64.8 with the `unused` linter) caught it. Local golangci-lint on this machine is v2.11.4 and the repo config is v1, so `golangci-lint run` errors out locally with "unsupported version of the configuration". Implication: for PRs that heavily refactor/delete, trust CI to catch dead code; OR install a v1 golangci-lint binary locally (e.g. `go install github.com/golangci/golangci-lint/cmd/golangci-lint@v1.64.8`) for pre-push. PR #26 hit this; fixed in `070bcd0`.
- **Post-squash-merge local cleanup gotcha.** `gh pr merge --squash --delete-branch` successfully squashed to main and deleted the remote branch, but the local-branch-delete step failed with `'main' is already checked out at '/home/rohan/ZenGarden/Bonsai'` because the CLI tried to fast-forward the local sibling worktree's main and that worktree was on main. Remote branch deletion also failed silently in the same call — needed a follow-up `git push origin --delete ui-ux-testing`. Check `gh pr view --json state` + `git ls-remote origin | grep <branch>` after any squash-merge from a non-main worktree to confirm both halves completed. Confirmed again 2026-04-21 × 4 during Plan 20 (#28, #29, #31, #40, #41 all hit the same pattern) — always need manual `git push origin --delete <br>` + `git worktree remove -f -f <path>` + `git branch -D <br>`.
- **Claude Code `statusLine.command` runs with different `$PWD` than hook commands.** The project-wide walk-up-to-`.bonsai.yaml` wrapper used for hooks (`bash -c 'r="$PWD"; while [ "$r" != "/" ] && [ ! -f "$r/.bonsai.yaml" ]; do r=$(dirname "$r"); done; bash "$r/<sensor>.sh"'`) **fails silently** when used for statusLine — walk terminates at `/` with no match. Use an **absolute path at install time** for statusLine (`"command": "bash /abs/path/to/statusline.sh"`). Confirmed 2026-04-22 when debugging why the project-level statusLine wasn't rendering. Also: statusLine config isn't hot-reloaded — requires `/clear` or restart to pick up settings.json changes.
- **Subdirectory launch determines which `.claude/settings.json` is live.** When Claude Code is launched from `station/`, the effective project settings file is `station/.claude/settings.json`, not the repo-root `.../.claude/settings.json`. The repo-root one is ignored. Hit this 2026-04-22 while wiring the statusLine stanza — put it in the wrong file and spent a cycle diagnosing. Check `pwd` at the time of the Claude session start before deciding which `.claude/settings.json` to edit.
- **golangci-lint binary Go-version coupling.** The `version: X.Y` input on `golangci/golangci-lint-action` resolves to a specific pre-built binary. That binary must have been built with a Go version ≥ the repo's target. Moving a repo to Go 1.25 required golangci-lint v2.11.4+ (older v2.1 binary was still built on Go 1.24 and errors with `can't load config: the Go language version (go1.24) used to build golangci-lint is lower than the targeted Go version (1.25.8)`). Rule: when bumping Go major in go.mod, also pin golangci-lint-action to a version whose release notes say "built with Go ≥ target". Track latest v2.x: https://github.com/golangci/golangci-lint/releases. Hit this 2026-04-21 across PR #28 / #29 during Plan 20.
- **Agent worktrees base off origin/main, not local main.** `Agent(isolation: "worktree")` fetches origin/main as the worktree base — ANY uncommitted work on the local-only main branch is invisible to the agent and won't be in its PR. To ship local-only work, create a dedicated branch from the local tip, push, PR, merge first — THEN dispatch agents. Learned 2026-04-21 during Plan 20 pre-flight: my 65cfdd6 chore commit was on local main only; first Go-bump PR #28 came back clean (2 lines) precisely because agent worktree didn't see it. Had to ship as separate PR #30 before resuming.
- **GoReleaser Homebrew step is silently PAT-dependent.** Release workflow succeeds up to GitHub Release publish, then fails at brew formula step if `HOMEBREW_TAP_TOKEN` is expired/missing — but the GitHub Release (with binaries + checksums) is **already published** by the time brew runs. So a tag push can ship a partial release: binaries live, Homebrew stale. Error signature: `GET https://api.github.com/repos/LastStep/homebrew-tap: 401 Bad credentials` + `homebrew formula: could not get default branch`. Recovery: rotate PAT, set `HOMEBREW_TAP_TOKEN` secret on main repo (NOT tap repo), then either (a) add `workflow_dispatch:` to `release.yml` and retry, OR (b) manually push formula via `gh api -X PUT /repos/LastStep/homebrew-tap/contents/Formula/bonsai.rb` with SHAs from `checksums.txt` on the GH Release. Path (b) is safer for one-off recovery on an already-published release (path (a)'s `goreleaser release --clean` on existing tag risks recreating the GH Release with new timestamps, breaking download caches). Hit 2026-04-22 during v0.2.0 ship; chose path (b). Filed workflow_dispatch addition + PAT expiry calendar as backlog items.
- **Session-start catalog generator diffs are load-bearing, not junk.** When session starts with `website/public/catalog.json` + 2 mdx pages modified and untracked, DON'T dismiss as "parallel pipeline regen ran out-of-band" (my 2026-04-22 assumption). On v0.2.0 audit, those exact diffs were the audit agent's output from running `node website/scripts/generate-catalog.mjs` during inventory — they were the FIX for the sensor-count-12→13 problem. Lesson: when session-start `git status` shows modified website catalog files AND you're about to run a catalog-related audit, check if a prior audit agent ran the generator. Would've saved a cycle of "do I rerun?" deliberation.
- **For tag-triggered release retries, GoReleaser `--clean` is destructive on existing releases.** `goreleaser release --clean` wipes `dist/` and recreates everything — against an already-published GH Release, this deletes+recreates the release (new `published_at` timestamp, new asset URLs may break caches for users mid-download). When retrying a failed release step: (1) add `workflow_dispatch:` to `release.yml` FIRST, (2) inside GoReleaser config add `--skip=archive,build,publish=false` flags for targeted retry, OR (3) just do the failed step manually via API (like the Homebrew path above). Lesson: always add `workflow_dispatch:` to any tag-triggered release workflow BEFORE the first release, so post-hoc retries are possible without re-tagging.
- **Direct-to-main polish commits accumulate silent gofmt/lint drift.** Fast-iter UX convention ships polish commits direct-to-main without PR — historically `ci.yml` only ran on `pull_request`, not `push`, so gofmt/lint regressions sat silently on main until the next PR tripped over them. Hit 2026-04-22 on Plan 24 PR #58 lint: `observe.go:466` + `planted.go:423` had been unformatted since the 2026-04-22 dogfood polish run. **Fixed at source 2026-04-22 via `ef55f4f`**: `.github/workflows/ci.yml` now triggers on `push: branches: [main]` in addition to `pull_request`. Direct-to-main pushes now run test/lint/govulncheck. Caught my own follow-on breakage within 5 minutes (see `git add` trap note below), confirming the widening pays off immediately.
- **`git add <file>` is all-or-nothing — pulls in unrelated WIP hunks from parallel sessions.** When working alongside a parallel session's uncommitted WIP in the same file, `git add <path>` stages EVERY modified hunk in that path, not just the ones you edited. 2026-04-22: committed `ef55f4f` intending to drop one `offset++` in `cmd/add.go:690`, but `git add cmd/add.go` also picked up Plan 23 WIP's `os` import + `BONSAI_ADD_REDESIGN` env gate calling an uncommitted `runAddRedesign` — broke main build on the newly-enabled push-CI within minutes. **How to apply:** When parallel-session WIP exists in a file you need to edit, either (a) stash the WIP first (`git stash push -u -- <paths>`), make your edit, commit cleanly, then restore WIP; or (b) use `git add -p <file>` to stage only your hunks; or (c) `git diff --staged` before every commit to verify scope. Rule: when working tree has parallel-session WIP, never use `git add <file>` without verifying the staged diff matches intent.
- **Parallel sessions can spontaneously switch your branch.** During 2026-04-22 wrap-up the parallel Plan 23 session created `feat/plan-23-phase-1-add-cinematic` from my `7c1dd49` tip and checked me out onto it mid-sequence — mid-commit staging picked up every uncommitted WIP file as staged-new-file. Switching back to main dropped those and reverted my in-progress wrap-up edits (had to re-apply Status/Backlog/memory/Log changes). **How to apply:** during wrap-up, commit bookkeeping in small batches quickly; always check `git branch --show-current` immediately before any commit when working alongside parallel sessions; if you see a branch you didn't create, unstage with `git restore --staged .` and `git switch main` first.

- **Parallel session can re-stage work BETWEEN your `git status` recheck and your `git add` — explicit-path `git add` is not enough.** 2026-04-23 wrap-up: pre-commit `git status --short` showed only my 4 paths modified (no Plan 29 entry). Next tool call was `git add <4 explicit paths>` + `git commit`. Commit `c8d66f8` captured a 5th rename (`station/Playbook/Plans/{Active => Archive}/29-init-add-bug-bundle.md`) that parallel session re-staged in the window between my recheck and my `git add`. The explicit-path `git add` didn't cause it — once in the index, anything already staged rides the next `git commit` regardless of whether you added it. Lost 1 round-trip reverting. **How to apply:** when working alongside a parallel session, run `git diff --cached --stat` AS PART OF the same command that does `git commit` (single bash call: `git add <paths> && git diff --cached --stat && git commit ...`), AND inspect the stat before trusting the commit. If stat shows more files than you added, abort with `git restore --staged .` + re-stage. Better: use `git commit -o <paths>` (commit-only) flag which restricts the commit to explicit pathspec regardless of other staged content — the `-o` option was designed exactly for this case.
- **Dispatched agents' Edit tool writes to absolute paths, not worktree-relative.** 2026-04-22 Plan 26 dispatch: agent's initial Edit calls landed in the main worktree at `/home/rohan/ZenGarden/Bonsai/...` instead of its worktree at `/home/rohan/ZenGarden/Bonsai/.claude/worktrees/agent-a0807188/...`. Agent self-recovered via `git diff > /tmp/patch` + `cd <worktree> && git apply /tmp/patch` + `git checkout -- <paths>` on main to revert — but we lucked out that the agent noticed before committing. **How to apply:** in agent dispatch prompts, include an explicit step at the top: "your working directory is `{worktreePath}` — verify via `pwd` and always use worktree-relative paths (or prefix absolute paths with that worktree root)". Also: post-dispatch pre-merge, always run `git status` on main worktree to confirm clean before `gh pr merge`.

## Feedback

<!-- User corrections and confirmed approaches that persist across sessions. Only record what isn't already in CLAUDE.md, workflows, or protocols. -->

- **Backlog.md at session start is a P0 *scan*, not a full read.**
    - **Why:** During the 2026-04-17 context audit, the CLI session-start payload was ~4k heavier than the app's, and the delta was traced to my having `Read` Backlog.md in full (~200 lines, ~4k tokens) when session-start protocol explicitly says "scan, look for P0 items only." Full backlog review is the backlog-hygiene routine's job. On a 200k window that's ~4% of headroom burned for zero benefit.
    - **How to apply:** At session start, use `Grep -n "^## P0" Playbook/Backlog.md` (or read with `limit:` bracketed to the P0 section) instead of `Read` on the whole file. Only full-read Backlog when explicitly running backlog-hygiene or planning across multiple groups.

- **Autonomous Tier-1 bundle dispatch works — user prefers it for small P2 knock-offs while they dogfood a separate track.**
    - **Why:** 2026-04-22 session: user was about to dogfood the Plan 23 `bonsai add` cinematic UI and asked whether to start remove/update redesign now or "knock something off easily." Recommended dogfood-first + autonomous P2 bundle; user said "take up backlog tasks that you can knock off autonomously. follow issue to implementation." Plan 26 (4 independent P2 items) went end-to-end w/ zero user intervention — analyze → plan → dispatch → review → merge → close-out — in one session, zero-finding review, 6/6 CI green. User-confirmed pattern for similar future asks.
    - **How to apply:** When user is busy w/ a parallel track (dogfooding, design exploration, external work) and asks for "autonomous" or "knock-offs," default to bundling ~3-5 well-scoped independent P2 items into one Tier-1 patch plan, single worktree dispatch, independent code-review agent, same-session merge. Scope boundary: Tier-1 only (no arch change, no cross-track dependency), items must have crisp file-level specs, and no item should touch the track the user is dogfooding in parallel (would invalidate their feedback). Matches existing "Fast iteration beats process for UX work" rule inversely: slow, rigorous PR loop for code-correctness-heavy work; user is fine with it as long as *they* aren't waiting on reviews.

- **Parallel dispatch pattern for Tier-2 multi-phase plans works when phases are file-disjoint — user expects no quality drop.**
    - **Why:** 2026-04-23 session: Plan 28 Phase 2 + Phase 3 (list + guide cinematic) were file-disjoint (`internal/tui/listflow/` + `cmd/list.go` vs `internal/tui/guideflow/` + `cmd/guide.go`). User confirmed "okay parallel, but no drop on quality of work." Ran implementing agents in parallel (~8 min each), independent code-reviewers in parallel, fix-agents in parallel. Both PRs PASS-WITH-MINORS, zero blockers, 6/6 CI green, merged sequentially with first-landed forcing trivial auto-rebase on second. End-to-end in one session; quality not compromised because each phase got the same 4-step loop (implement → review → fix → merge) just pipelined.
    - **How to apply:** When a plan has multiple phases that don't touch the same files (verify via `grep -r <file> <other-phase-spec>` or read the Files Touched sections), default to parallel `Agent(isolation: worktree)` dispatch. Pre-dispatch checklist: (1) confirm file-disjoint, (2) confirm no shared type/signature changes between phases (signature extension in Phase N must ship BEFORE Phase N+1 dispatches, else both agents touch it), (3) dispatch both with SAME quality bar prompt (plan reference, verification checklist, PR creation, no-merge-just-draft), (4) review each PR with its own independent code-review agent in parallel (they don't cross-contaminate), (5) dispatch fix-agents in parallel, (6) merge sequentially (first-landed, second-rebases). Risk: each review takes full effort so reviewer-load doubles; if you're at high context %, sequential is safer. Rule of thumb: parallel below 40% context, sequential above.

### Durable UX preferences (2026-04-17 dogfooding)

> Durable learnings captured during dogfooding sessions. These describe what the user values in work you deliver and how they prefer to iterate. Extend this section when new patterns emerge — don't let it calcify.

#### On visual and UI work

- **Palette first, visuals second.**
    - **Why:** During the 2026-04-17 `bonsai init` review, user pushed back on "overall coloring" as the first complaint. Before any re-skinning, semantic tokens must exist — otherwise every color change becomes a codebase-wide find-replace.
    - **How to apply:** For any TUI change that touches color or style, check that semantic tokens (`ColorPrimary`, `ColorAccent`, etc.) exist and are used. Introduce them as a prereq step if missing.

- **Sleek and minimal over ornate.**
    - **Why:** User called the prior UI "bulky", "thrown together", and the flat `B O N S A I` banner "unprofessional". Default ASCII art, excessive padding, spaced-letter wordmarks, and busy layouts are taste-negative signals.
    - **How to apply:** Prefer tight wordmarks, compact boxes, meaningful glyphs over decoration. When in doubt, strip and measure — don't ornament.

- **Visible state over hidden state.**
    - **Why:** User noticed their project name disappeared after advancing to the description prompt, and that required-only ability sections silently skipped. Both broke their sense of "I did something — where did it go?"
    - **How to apply:** Any answered prompt, selected item, or auto-processed section should leave visible evidence on screen. Don't rely on Huh's default clear-on-submit behavior — print a summary line afterward.

- **Rich guidance, not cramped.**
    - **Why:** User said "the next to do hints should be more rich, own their own space, and can be bit more verbose" — terse one-liners at the bottom of panels feel disrespectful of the moment.
    - **How to apply:** Next-steps, file structure views, and any screen that showcases core value should get dedicated visual real estate and substantive copy. Don't default to a single `Hint()` call when the user just finished something significant.

- **TUI should redraw, not stack.**
    - **Why:** User complained that the review panel, generate confirmation, and success messages all piled on top of each other during init. Their mental model is that each major step should feel like a new canvas.
    - **How to apply:** On major step transitions (review, generate, complete), plan for AltScreen or explicit clear/redraw. Don't treat TUI output as a scrollable log.

#### On planning and iteration

- **Fast iteration beats process for UX work.**
    - **Why:** User said "we don't need PRs for this, first we test locally" — for taste-heavy design work, the PR review loop is too slow to be useful until the visual direction is settled.
    - **How to apply:** When user signals "test locally," still use worktrees for isolation but commit directly to main (or merge to main locally). Skip PR creation. Save the PR flow for code-correctness-heavy work.

- **Pick scope pragmatically, foundations first.**
    - **Why:** User dropped 11 items at once (Group F) and said "pick some of these up." They trust me to sequence — which means wrong sequencing wastes their time.
    - **How to apply:** Group by dependency (foundations before consumers — e.g., palette before banner) and visible-win density. Defer architectural rewrites until taste has settled on smaller items. State the deferred scope explicitly so the user can redirect.

- **Propose scope before writing the plan.**
    - **Why:** Plan documents are verbose; scope disagreements at the plan stage cost a rewrite. A scope summary is ~10x cheaper to revise.
    - **How to apply:** For any taste-heavy or ambiguous task, send a scope summary (picked / deferred / rationale) and ask "OK to proceed?" before drafting the plan file.

- **Log findings as they surface, don't batch.**
    - **Why:** User said "add things to backlog as we go along, under ui ux testing category" — they want a running tally in real time, not a post-hoc summary.
    - **How to apply:** Maintain Group F (or equivalent) in `Playbook/Backlog.md` during any testing session. Each finding: category tag, Group F tag, specific fix options if known, source attribution. Don't fix inline and don't wait until the session ends.

#### On communication

- **Concise and direct wins.**
    - **Why:** User makes fast decisions with minimal elaboration ("b, but i will init myself"). Long hedged answers waste their attention.
    - **How to apply:** Short options, direct recommendations, no preamble. Mirror their energy — if they write two sentences, don't respond with five paragraphs.

- **Surface incidental findings proactively.**
    - **Why:** The `go install` binary-name bug was discovered while setting up their test environment, not while looking for it. If I'd silently renamed the binary, the bug would have stayed hidden.
    - **How to apply:** When you hit a workaround while doing setup/chores, explicitly flag it as a finding. Don't normalize broken behavior into your flow.

## References

<!-- Pointers to external resources not documented elsewhere in the project. -->

- **Foundational research docs** — Anchor for methodology/concept decisions. Reference these when improvements need grounding in the core philosophy (ambient behavior, meta-layer, talents, evals).
    - [Research/RESEARCH-landscape-analysis.md](../../Research/RESEARCH-landscape-analysis.md) — How Bonsai compares to GSD/ECC/others; Bonsai's identity/coordination layer positioning
    - [Research/RESEARCH-concept-decisions.md](../../Research/RESEARCH-concept-decisions.md) — Ambient vs. command-driven, authority hierarchy, catalog ownership, talents taxonomy
    - [Research/RESEARCH-eval-system.md](../../Research/RESEARCH-eval-system.md) — Eval system concept: scenarios, evaluators, benchmarks for methodology rigor
    - [Research/RESEARCH-trigger-system.md](../../Research/RESEARCH-trigger-system.md) — Trigger section design research
    - [Research/RESEARCH-uiux-overhaul.md](../../Research/RESEARCH-uiux-overhaul.md) — UI/UX overhaul research (Plan 14/15 origin)
    - [Research/RESEARCH-proof-of-bonsai-effectiveness.md](../../Research/RESEARCH-proof-of-bonsai-effectiveness.md) — Pre-registration doc for building a proof-of-work story for OSS launch. Cut-over `4dfd3f4` (2026-04-14). Claims, sources (codeburn / transcripts / git), control options, methodology, risks. Pick up when ready — user answers §10 first.
